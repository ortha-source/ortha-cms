/** The three tables this plugin owns — the Drizzle schema its migrations build. */
export { protectionRules } from './protection-rules';
export { reviewRequests } from './review-requests';
export { reviewApprovals } from './review-approvals';
