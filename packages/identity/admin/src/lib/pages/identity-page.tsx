import { Button } from '@ortha-cms/design-system';

/**
 * Placeholder landing page for the identity admin plugin. User, role, and
 * access-management screens land here in later tickets.
 */
export function IdentityPage() {
    return (
        <div>
            <h1>Identity</h1>
            <p>User, role, and access management lives here.</p>
            <Button>Get started</Button>
        </div>
    );
}
