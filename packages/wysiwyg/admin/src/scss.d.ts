/**
 * The Tiptap template ships its styles as SCSS and imports them from its own
 * components. Vite compiles those imports; TypeScript only needs to know the
 * specifier resolves to something.
 */
declare module '*.scss';
