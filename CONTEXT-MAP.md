# Context Map

A single index of _what lives where_ and _what the words mean_. When an agent or
a new engineer needs to find the right package or decode a term, start here.

## Project map

Every app and package, one line each. Each package's own `AGENTS.md` has the
detail.

### Apps (`apps/`)

| Project      | Package name | What it is                                                                      |
| ------------ | ------------ | ------------------------------------------------------------------------------- |
| `admin`      | —            | React 19 + Vite SPA. The admin UI. Composes admin plugins via `createAdmin`.    |
| `server`     | —            | NestJS API. Composes server plugins via `createServer`. Owns `ortha.config.ts`. |
| `admin-e2e`  | —            | Playwright Page-Object e2e suite for the admin SPA (`/api` mocked).             |
| `server-e2e` | —            | In-process testcontainer + supertest e2e suite for the API.                     |

### Packages (`packages/`)

| Project                      | Package name                           | Role                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `bootstrap/admin`            | `@orthacms/bootstrap-admin`            | **Host.** `createAdmin({ plugins })` — React root, router, providers.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `bootstrap/server`           | `@orthacms/bootstrap-server`           | **Host.** `createServer({ plugins })` — Nest app, `/api` prefix, validation.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `database`                   | `@orthacms/database`                   | **Plugin.** One Drizzle/`pg` connection via DI, plus the shared tactical-DDD infrastructure (`UnitOfWork`, the transactional outbox, the `DomainEvent` contract). Owns exactly **one** table — `outbox_events` — and its migrations: the sanctioned exception to "no schema".                                                                                                                                                                                                                                      |
| `identity/server`            | `@orthacms/identity-server`            | **Plugin.** The foundational one: auth (bcrypt login, DB-backed sessions, invite acceptance), RBAC (global roles → permissions, seeded and protected), SSO over the `SsoProvider` port, API-token minting, and user preferences. Owns that schema + migrations. **Not** workspaces — those live in `workspaces/server`.                                                                                                                                                                                            |
| `identity/admin`             | `@orthacms/identity-admin`             | **Plugin.** Login + accept-invite pages, auth client state.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `identity/domain`            | `@orthacms/identity-domain`            | The framework-free core of identity's SSO seam: the `SsoProvider` port, the normalised `SsoProfile`, the shared errors, the open-redirect guard, and the conformance kit every adapter must pass ([ADR-0013](docs/adr/0013-sso-provider-port.md)). Imports nothing at all.                                                                                                                                                                                                                                         |
| `identity/provider-oidc`     | `@orthacms/identity-provider-oidc`     | Generic **OpenID Connect** adapter — authorization code + PKCE, ID tokens verified against published keys — plus thin presets for the vendors people name (Okta, Auth0, Keycloak, Google, Entra ID, …). One adapter covers most of the market.                                                                                                                                                                                                                                                                     |
| `identity/provider-github`   | `@orthacms/identity-provider-github`   | GitHub sign-in (github.com and Enterprise Server). Its own package because GitHub is **not** OIDC: there is no ID token, so `complete` spends the code for an access token and reads the profile back.                                                                                                                                                                                                                                                                                                             |
| `identity/provider-saml`     | `@orthacms/identity-provider-saml`     | SAML 2.0 — HTTP-Redirect out, HTTP-POST back. The reason `callbackMethod` is in the port's descriptor from the first release: a second protocol shape needed no change to the seam.                                                                                                                                                                                                                                                                                                                                |
| `identity/provider-fake`     | `@orthacms/identity-provider-fake`     | Scripted identity provider — no network, no tenant, the same answer every run. **Shipped, not test scaffolding**: how `server-e2e` drives the whole SSO handshake, and how a contributor exercises the sign-in page with no IdP to hand.                                                                                                                                                                                                                                                                           |
| `users/server`               | `@orthacms/users-server`               | **Plugin.** Member management API (list/invite/edit/disable/sessions).                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `users/admin`                | `@orthacms/users-admin`                | **Plugin.** Members roster, invite flow, user detail tabs (incl. a self-only **Preferences** tab: colour theme Light/Dark/System via `/api/preferences`) + app-wide theme sync.                                                                                                                                                                                                                                                                                                                                    |
| `workspaces/server`          | `@orthacms/workspaces-server`          | **Plugin.** The tenancy boundary — workspaces, memberships, and the `workspace_content` access grants — plus the `WorkspaceGuard` every workspace-scoped route in other plugins names. Owns those three tables and their migrations. The **reference implementation of [ADR-0003](docs/adr/0003-tactical-ddd-inside-plugins.md)**: when a layering question is ambiguous, this package is the worked example.                                                                                                      |
| `workspaces/admin`           | `@orthacms/workspaces-admin`           | **Plugin.** Workspace table + create wizard + the workspace shell (`/workspaces/:id/*`, injects its nav into the app sidebar). Defines `WORKSPACE_NAV_SLOT` / `WORKSPACE_SECTION_SLOT` / `WORKSPACE_ROUTE_SLOT`.                                                                                                                                                                                                                                                                                                   |
| `content/domain`             | `@orthacms/content-domain`             | The content **shared kernel**: the entry-status state machine (`draft ⇄ published`), the rich-text document model, and field-value validation — the rules both runtimes must apply identically. Zero dependencies, and per [ADR-0003](docs/adr/0003-tactical-ddd-inside-plugins.md) the one sanctioned FE↔BE code share.                                                                                                                                                                                          |
| `content/server`             | `@orthacms/content-server`             | **Plugin.** The content model: the code-defined `collection()` / `single()` DSL turned into physical Postgres tables, the runtime registry, entry CRUD + publish + revisions, saved views, and the token-authenticated **public REST API** (`/api/v1`) the GraphQL and MCP surfaces adapt. The generated tables' migrations are **host**-owned; only `saved_views` / `saved_view_defaults` ship from here (via a second `ContentViewsPlugin` entry).                                                               |
| `content/admin`              | `@orthacms/content-admin`              | **Plugin.** Content Library: the app sidebar's Content section (collapsible Collections/Pages + favorites), ⌘K search palette, records list, entry editor. Defines **15** extension slots — five around the records list (toolbar, menu, columns, filter fields, bulk actions), eight around the editor (header, menu, tabs, sidebar widgets, params, field controls, pre-save, publish guard), one on the revision view, one route-level overlay — and fills none of them itself.                                 |
| `content/graphql`            | `@orthacms/content-graphql`            | **Plugin.** The public content API over GraphQL (`POST /api/v1/graphql`, `GET` for SDL). An adapter over content-server's `public-api/` — same tokens, guards, scopes and visibility rules; the schema is built per workspace grant set (ADR-0008).                                                                                                                                                                                                                                                                |
| `i18n/server`                | `@orthacms/i18n-server`                | **Plugin.** Content localization: binds content's `CONTENT_ENTRY_EXTENSION` port (row-per-locale scoping, create stamping, shared-field sync, locale filters) + `/api/i18n` (locales, panels, translations) + `/api/insights/i18n/coverage` (translation coverage — it owns the configured locale set, so it owns the question). Owns no tables.                                                                                                                                                                   |
| `i18n/admin`                 | `@orthacms/i18n-admin`                 | **Plugin.** Locale UI via the content library slots: locale switcher, Locales column, editor locale panel, locale filters — plus the Insights page's Translation coverage card. No routes.                                                                                                                                                                                                                                                                                                                         |
| `wysiwyg/admin`              | `@orthacms/wysiwyg-admin`              | **Plugin.** Rich-text editing: owns the control every `richtext` field renders — a content preview that expands into a TipTap editor filling the record's work area (callouts, tables, columns, resizable images/video). Fills content's `ENTRY_FIELD_CONTROL_SLOT`; defines `WYSIWYG_MEDIA_SLOT` for media sources. No routes, no server side.                                                                                                                                                                    |
| `media/domain`               | `@orthacms/media-domain`               | The **storage port** — the `StorageProvider` interface every adapter speaks and the one error `get` rejects with. No dependencies at all, which is what lets an adapter depend on the port without dragging in NestJS.                                                                                                                                                                                                                                                                                             |
| `media/server`               | `@orthacms/media-server`               | **Plugin.** The media bounded context — folders and assets, owning `media_folder` / `media_asset` and its migrations. Bytes never live in the database: a row carries a `storage_key` + `storage_provider` pointer and the blob is held by whichever `StorageProvider` the composition root registered.                                                                                                                                                                                                            |
| `media/admin`                | `@orthacms/media-admin`                | **Plugin.** Media Library (workspace nav), the entry editor's Media tab, and the rich-text editor's media sources (`WYSIWYG_MEDIA_SLOT`).                                                                                                                                                                                                                                                                                                                                                                          |
| `media/provider-local`       | `@orthacms/media-provider-local`       | The **default** storage provider: blobs streamed to a directory on disk. Node built-ins and the port, nothing else.                                                                                                                                                                                                                                                                                                                                                                                                |
| `media/provider-s3`          | `@orthacms/media-provider-s3`          | **S3-compatible** storage, written endpoint-first rather than AWS-first — `endpoint` + `forcePathStyle` is the whole difference between AWS, Cloudflare R2, MinIO, Spaces, B2, Wasabi and the rest. One adapter, not ten.                                                                                                                                                                                                                                                                                          |
| `media/provider-azure`       | `@orthacms/media-provider-azure`       | Azure Blob Storage — the one major object store with no S3 compatibility at all (different protocol, signature and container model), so it needs its own adapter.                                                                                                                                                                                                                                                                                                                                                  |
| `media/provider-gcs`         | `@orthacms/media-provider-gcs`         | Google Cloud Storage, natively. Often unnecessary: GCS speaks the S3 XML API in interoperability mode, which `provider-s3` already reaches.                                                                                                                                                                                                                                                                                                                                                                        |
| `media/provider-vercel-blob` | `@orthacms/media-provider-vercel-blob` | Vercel Blob. The smallest adapter, and the one with a deployment caveat rather than a code one: Vercel Blob has a single access mode, `public`, so every stored blob is world-readable to anyone holding its URL.                                                                                                                                                                                                                                                                                                  |
| `media/provider-memory`      | `@orthacms/media-provider-memory`      | In-memory `StorageProvider` — blobs in a `Map`, plus `keys()` / `totalBytes()` / `clear()`. **Shipped, not test scaffolding**, for the same reason as the copilot's `provider-fake`.                                                                                                                                                                                                                                                                                                                               |
| `media/provider-testkit`     | `@orthacms/media-provider-testkit`     | The `StorageProvider` contract **as a runnable suite** — one export, `describeStorageProvider(name, harness)`, which every adapter's spec calls. The port's invariants, checked rather than described.                                                                                                                                                                                                                                                                                                             |
| `insights/admin`             | `@orthacms/insights-admin`             | **Plugin.** The Insights dashboard (workspace nav). Ships the page frame, the range picker, the widget card shell and the chart primitives — and **no widgets**: it defines `INSIGHTS_WIDGET_SLOT` / `INSIGHTS_SECTION_SLOT`, which `content/admin`, `media/admin` and `i18n/admin` fill. No server package; each contributor serves its own aggregates.                                                                                                                                                           |
| `copilot/domain`             | `@orthacms/copilot-domain`             | The copilot's framework-free core: the `ModelProvider` port, the tool contracts, the capability profile, the proposal contracts, the run event vocabulary, and the untrusted-content fence. Imports no framework and no vendor SDK.                                                                                                                                                                                                                                                                                |
| `copilot/server`             | `@orthacms/copilot-server`             | **Plugin.** `CopilotPlugin({ providers, resolve, config })` — the model seam plus the chat slice: the SSE run route, the bounded run engine, the capability profile, the model catalogue, the write path (a `propose` tool's change is recorded then applied immediately — ADR-0009), and the five tables it owns and migrates.                                                                                                                                                                                    |
| `copilot/admin`              | `@orthacms/copilot-admin`              | **Plugin.** Two surfaces onto one chat: the **non-modal docked window** (bottom-right, several at once behind a dock, `⌘J`) and the full-page **Agents view** at `/workspaces/:id/agents` — thread rail, one conversation, and a CMS ⇄ Agents switch in the workspace sidebar that returns you to the page you left. Both share the `fetch`-based SSE transport, the transcript with its tool-step disclosure and inline permission prompts, a per-turn model picker, and the proposal card that reports a change. |
| `copilot/provider-anthropic` | `@orthacms/copilot-provider-anthropic` | Native Claude adapter over `@anthropic-ai/sdk`. The default for tool-heavy work; probes the Models API for live capabilities.                                                                                                                                                                                                                                                                                                                                                                                      |
| `copilot/provider-openai`    | `@orthacms/copilot-provider-openai`    | Adapter for the OpenAI wire format with a configurable `baseUrl` — Ollama, vLLM, llama.cpp, LM Studio, LiteLLM, OpenRouter, Azure, OpenAI. Makes a local install a setting, not a fork.                                                                                                                                                                                                                                                                                                                            |
| `copilot/provider-fake`      | `@orthacms/copilot-provider-fake`      | Scripted, deterministic adapter. **Private test fixture — never published, never registered by a host**: how CI runs the copilot loop with no key and no network.                                                                                                                                                                                                                                                                                                                                                  |
| `transfer/domain`            | `@orthacms/transfer-domain`            | The transfer **kernel**: the document contract both ends of an export/import agree on, the format implementations (JSON / NDJSON / ZIP / CSV) behind one port, and the natural-key rules an import matches on. Framework-free.                                                                                                                                                                                                                                                                                     |
| `transfer/server`            | `@orthacms/transfer-server`            | **Plugin.** Content export and import (`/api/content/:type/export`, `/import`). Walks **one hop** out from the selected records — relations, files, locales — and writes back through the ordinary `EntryWriterService` (ADR-0014). Owns no tables; hand-rolls its ZIP container so the untrusted-archive limits are its own.                                                                                                                                                                                      |
| `transfer/admin`             | `@orthacms/transfer-admin`             | **Plugin.** Export from the entry ⋯ menu and the records selection bar, import from the collection toolbar. The export dialog's live counts run the same graph walk the export does; import is dry-run-then-apply. No routes.                                                                                                                                                                                                                                                                                      |
| `segments/domain`            | `@orthacms/segments-domain`            | The **segmentation kernel** — reader entitlements as one pure function over two lists. A segment is a named set of reader tags; an entry names the segments it admits and the segments it refuses; a deny wins and an empty allow list means everyone. Not RBAC, which answers a different question.                                                                                                                                                                                                               |
| `segments/server`            | `@orthacms/segments-server`            | **Plugin.** Owns `segments` + `entry_access` and compiles the decision into one SQL predicate through content's `CONTENT_READ_SCOPE` registry — so it covers REST, GraphQL and MCP at once. Also ships the segment tools and the copilot's propose/apply pair. Inert until an audience exists.                                                                                                                                                                                                                     |
| `segments/admin`             | `@orthacms/segments-admin`             | **Plugin.** Two surfaces and no more: the audience directory at `/segments`, and the entry editor's **Access** tab, where each audience gets one three-state control.                                                                                                                                                                                                                                                                                                                                              |
| `protection/domain`          | `@orthacms/protection-domain`          | The publication-protection **kernel** — the third gate on publish, as one pure function over a rule, the head revision and the votes recorded against it. Answers _may this person ship it now_, never whether the entry is complete: an approval belongs to a **revision**, so the next save stops it counting with no dismissal logic anywhere. Framework-free.                                                                                                                                                  |
| `protection/server`          | `@orthacms/protection-server`          | **Plugin.** The publication-protection rule surface: a per-content-type rule requiring N approvals before an entry may be published ([ADR-0017](docs/adr/0017-publication-protection.md)). Owns three tables — the rule, the review request, and the vote, which carries a `revision_id` and so expires on the next save with no dismissal logic. Addressed `(workspace, kind, slug)`, the same coordinate as `workspace_content`. Administrator-only, and inert until a rule exists.                              |
| `protection/admin`           | `@orthacms/protection-admin`           | **Plugin.** Publication protection in the entry editor — the review chip beside the title, the properties rail's **Review** block (approvers, with a stale one struck through naming the version it was given on), and the verdict that decides what the Publish button says, plus the bypass dialog behind it. Three contributions into the Content Library's slots and no page of its own; every one is silent on an unprotected type.                                                                           |
| `alarms/server`              | `@orthacms/alarms-server`              | **Plugin.** Content alarms: workspace rules that flag problems and never block a write (ADR-0015). Owns `alarm_rules` + `alarm_findings`; evaluates a rule — a stored records-list filter tree — through content's `EntryMatchQuery`, off entry lifecycle events on the outbox, plus an on-demand rescan and a periodic sweep.                                                                                                                                                                                     |
| `alarms/admin`               | `@orthacms/alarms-admin`               | **Plugin.** The workspace's alarms page and rule editor, plus three Content Library slot contributions: the entry rail's checks block, an optional records Checks column, and **"Save as rule"** in the records toolbar — the main way a rule gets created, from a filter someone already built.                                                                                                                                                                                                                   |
| `tools/server`               | `@orthacms/tools-server`               | The shared, transport-neutral agent **tool registry** and the one place a tool call is authorized. Two consumers — the MCP endpoint and the copilot's run loop — import its global `ToolsModule` so both see one instance. A tool declares its `surfaces`.                                                                                                                                                                                                                                                         |
| `mcp/server`                 | `@orthacms/mcp-server`                 | **Plugin.** The Model Context Protocol front door (`POST /api/v1/mcp`) — protocol, bearer auth, workspace resolution, JSON-RPC dispatch. Owns no tools and no registry; `content/server` contributes the public-API content CRUD set. Off unless `MCP_ENABLED=true`.                                                                                                                                                                                                                                               |
| `webhooks/domain`            | `@orthacms/webhooks-domain`            | The webhooks **kernel**: the subscribable event catalogue, the subscription filter, the signed envelope, the retry policy, and the URL policy that decides where this server may connect. Framework-free.                                                                                                                                                                                                                                                                                                          |
| `webhooks/server`            | `@orthacms/webhooks-server`            | **Plugin.** Outgoing webhooks on content changes. Its outbox subscriber only **queues** delivery rows; a worker claims them `FOR UPDATE SKIP LOCKED`, commits the claim, and then POSTs with no transaction open ([ADR-0016](docs/adr/0016-webhooks-deliver-from-a-queue.md)). Owns three tables. Administrator-only.                                                                                                                                                                                              |
| `webhooks/admin`             | `@orthacms/webhooks-admin`             | **Plugin.** The global `/webhooks` pages in the directory group: the endpoint list, the editor with its three "All …" filters, the one-time secret reveal, and the self-polling delivery log with "Send again".                                                                                                                                                                                                                                                                                                    |
| `activity/server`            | `@orthacms/activity-server`            | **Plugin.** Audit-event schema + read API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `api-tokens/admin`           | `@orthacms/api-tokens-admin`           | **Plugin.** Global API Tokens page (directory group): mint a bearer token over one _or more_ workspaces, reveal its secret once, revoke it. Token model + management API live in `identity/server`; the public API the token is spent against lives in `content/server`.                                                                                                                                                                                                                                           |
| `activity/admin`             | `@orthacms/activity-admin`             | **Plugin.** Global & user-scoped activity logs + home recent-activity panel.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `shell/admin`                | `@orthacms/shell-admin`                | **Plugin.** Authenticated chrome (left sidebar, layout, home dashboard). Defines `SIDEBAR_NAV_SLOT` / `SIDEBAR_SECTION_SLOT` / `SIDEBAR_FOOTER_SLOT` / `HOME_SECTION_SLOT` / `COMMAND_SLOT`.                                                                                                                                                                                                                                                                                                                       |
| `query-builder/admin`        | `@orthacms/query-builder-admin`        | Admin filter/query-builder UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `design-system`              | `@orthacms/design-system`              | shadcn/ui component library + Tailwind. Governed by the `shadcn` skill.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `utils/admin`                | `@orthacms/utils-admin`                | Shared admin plumbing: `apiClient`, `queryClient`, slots, error handling.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `utils/server`               | `@orthacms/utils-server`               | Shared server utilities — the `?filter=` query builder, Postgres error introspection, and the environment readers a host's `ortha.config.ts` validates `process.env` with (shared with the scaffolder's template).                                                                                                                                                                                                                                                                                                 |
| `nx`                         | `@orthacms/nx`                         | **Nx plugin.** Infers the `db:generate` / `db:migrate` + release targets; the database executors delegate to `@orthacms/cli`.                                                                                                                                                                                                                                                                                                                                                                                      |
| `cli`                        | `@orthacms/cli`                        | **The `ortha` command.** Build, run and migrate an installed app.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `create-ortha-app`           | `create-ortha-app`                     | **Scaffolder.** `npx create-ortha-app my-cms`; one template, no content types.                                                                                                                                                                                                                                                                                                                                                                                                                                     |

> Regenerate this table whenever an app or package is added/removed/renamed.
> `docs-guard` fails the build if you forget.

### Tooling (`tools/`)

| Project      | Package name | What it is                                                                              |
| ------------ | ------------ | --------------------------------------------------------------------------------------- |
| `docs-guard` | —            | Pins the countable claims in `ARCHITECTURE.md` and this file to the code they describe. |

## Glossary

- **Host** — the app shell (`bootstrap-admin` / `bootstrap-server`) that turns a
  plugin list into a running app. Owns no domain logic.
- **Plugin** — a unit of capability. Admin plugins contribute routes/slots;
  server plugins contribute a NestJS module + (optionally) schema & migrations.
- **`AdminPlugin` / `ServerPlugin`** — the plugin factory + contract for each
  runtime. See [`ARCHITECTURE.md`](ARCHITECTURE.md).
- **Slot** — a named UI extension point (e.g. `SIDEBAR_NAV_SLOT`) a plugin
  defines and others contribute into, with no direct coupling.
- **DI port** — the server-side extension idiom: a `Symbol` token + interface a
  foundational plugin declares and injects `@Optional()`, bound by an
  implementing plugin (e.g. `CONTENT_ENTRY_EXTENSION`, declared by content-server
  and bound by i18n-server). The inversion keeps the package graph acyclic.
  A token binds **once** — Nest has no multi-provider — so a seam that must take
  any number of contributions is a **registry** instead (content's
  `CONTENT_READ_SCOPE`, the copilot's tool registrar).
- **Localization / locale group** — an `i18n: true` content type stores one row
  per locale; sibling rows share a `locale_group_id`. Locale behavior lives in
  `@orthacms/i18n-server` (behind `CONTENT_ENTRY_EXTENSION`), not in content.
- **`@InjectDatabase()`** — DI token for the shared Drizzle connection from
  `@orthacms/database`.
- **Resolve-from-source** — workspace packages are consumed straight from
  `src/index.ts` with no build step (`@orthacms/source` TS condition).
- **RBAC** — role-based access control. One global role per user (`admin`,
  `contributor`, `viewer`) → permission keys (`resource:action`), enforced by
  `@RequirePermissions` + `PermissionsGuard` (server) and `useHasPermission`
  (admin).
- **Workspace** — a tenant/grouping. **Membership** — an M:N user↔workspace link.
- **API token / workspace bucket** — a long-lived bearer credential for the
  **public content API** (`/api/v1/...`), minted in the admin's API Tokens page.
  Its _bucket_ is the set of workspaces it may read (`api_token_workspaces`, one
  or more); a request picks one with `X-Workspace-Id`. Model + management API:
  `identity/server`; the read API and its guards: `content/server`
  (`public-api/`). The **same** token also authenticates the GraphQL protocol at
  `/api/v1/graphql` (`content/graphql`) — one credential, two protocols.
- **Webhook endpoint / delivery** — a URL this CMS POSTs to when content
  changes, and one event's journey to one endpoint. An endpoint carries three
  filters (workspaces × event kinds × content types) where an **empty set means
  "everything, including what does not exist yet"** — the opposite of an API
  token's bucket, because a filter is not a grant. Delivery is at-least-once and
  unordered; a receiver deduplicates on `X-Ortha-Event-Id` and verifies the
  HMAC in `X-Ortha-Signature`.
- **MCP** — the Model Context Protocol, how an AI agent discovers and calls what
  a system can do. Ortha is an MCP **server** (`@orthacms/mcp-server`,
  [ADR-0006](docs/adr/0006-cms-as-an-mcp-server.md)); the copilot separately
  plans to be an MCP **client** of an operator's systems — opposite directions,
  different features.
- **Tool / tool registry** — a named, permission-gated capability an agent can
  invoke (`content_create`), and the transport-neutral catalogue holding them. A
  plugin contributes tools via the `TOOL_PROVIDER` port; `ToolRegistry.call` is
  the single place a tool call is authorized, the analogue of
  `@RequirePermissions` on a route.
- **Transfer** — one export or import. Its **document** is the format-neutral
  shape both ends agree on (`@orthacms/transfer-domain`); its **depth** is how
  far past the selected records it reaches — one hop, by decision
  ([ADR-0014](docs/adr/0014-transfer-as-a-separate-plugin.md)).
- **Natural key** — the field values that identify a record across
  installations (a `slug`, an `sku`, an `email`), as opposed to its row id,
  which means nothing outside the database it came from. What an import matches
  on to tell "update that record" from "add a new one".
- **Activity event** — an append-only audit row. It is **not** written in the
  mutation's transaction: the mutation commits a **domain event** to the outbox
  in that transaction, and `activity`'s subscriber turns the audited kinds into
  rows once the dispatcher delivers them. Delivery is at-least-once, so the
  insert is keyed on the event id `ON CONFLICT DO NOTHING`.
- **`workspace_content`** — an access-control mapping (workspace → code-defined
  content type), **not** a content-storage table: it is the workspace's declared
  content surface, and `ContentGrantGuard` refuses any content route naming a
  type the open workspace was never granted. Entries themselves live in the
  tables generated from the host's content types. Owned by `workspaces/server`.
- **Segment / audience** — a named set of reader tags, and the reader-facing
  half of access: an entry names the segments it admits and the segments it
  refuses, a deny wins, and an empty allow list means everyone. Separate from
  RBAC and `workspace_content`, which answer who may _touch_ content rather than
  who may _read_ it. Inert until an audience exists.
- **Alarm rule / finding** — a workspace-defined check over content and what it
  flagged. A rule stores the records-list **filter tree verbatim** and is
  evaluated through content's own `EntryMatchQuery`, so it can only ever mean
  what the list means. It never blocks a write
  ([ADR-0015](docs/adr/0015-alarms-are-non-blocking.md)).
- **Storage provider** — the `StorageProvider` port (`@orthacms/media-domain`)
  a deployment binds exactly **one** implementation of
  ([ADR-0012](docs/adr/0012-one-storage-provider-per-deployment.md)). Asset rows
  hold a `storage_key` + `storage_provider` pointer; the bytes never touch the
  database.
- **SSO provider** — the `SsoProvider` port (`@orthacms/identity-domain`) an
  adapter implements (`provider-oidc`, `-github`, `-saml`, `-fake`), so a
  protocol can be added without identity or the adapters depending on each other
  ([ADR-0013](docs/adr/0013-sso-provider-port.md)).
- **ADR** — Architecture Decision Record. See [`docs/adr/`](docs/adr/README.md).

## Skills (machine-readable context)

Authoring conventions are encoded as skills under `.agents/skills/` and
`.claude/skills/`: `server-plugin`, `admin-plugin`, `accessibility`,
`admin-e2e`, `server-e2e`, `shadcn`, and `agent-browser`.
Prefer the skill over reverse-engineering a convention from code.

<!-- TODO: add domain/business glossary terms (non-technical) as the product grows. -->
