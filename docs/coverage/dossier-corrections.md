# Dossier corrections found during coverage triage

Triage reads each dossier against the code that implements it, so it turns up
claims that have drifted. Where an invariant itself is wrong, it is recorded as
`stale` in `judgments/<pkg>.json` and the dossier gets fixed. This file collects
the rest — errors in the surrounding prose, checklists and counts, which the
ledger has no state for but which mislead the next reader just as much.

Nothing here is a code defect. Code defects belong in the gap list.

| Package          | Where                                          | What is wrong                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| identity         | Invariant I-19                                 | Says the API-token routes are reachable "only with an administrator's session". The gate is the `tokens:read` / `tokens:manage` permissions, not the admin role — `api-tokens-management.spec.ts` → `permission separation` proves a custom role holding `tokens:read` can list them. Reword to name the permissions.                                                                                                                                                                                                                                                                                                                                                                                                      |
| identity         | Testing checklist                              | Counts `apps/server-e2e/src/server/auth/*` as "15 files"; there are 22. The admin-e2e count (13) is right.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| mcp              | §13.4                                          | Asserts concrete tool counts ("13 tools" for `read`, "23 tools" for `full`). The e2e deliberately checks membership rather than counts, so these numbers are pinned by nothing and will drift silently as tools are added.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| mcp              | Section numbering                              | Invariants render as §12 and the testing checklist as §13, where most dossiers put them at §13/§14. Cosmetic, but it breaks cross-references written against the usual numbering.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| transfer         | Invariant I-04                                 | Ambiguous: "a depth-1 entry's reference" reads either as references _to_ depth-1 records or references _held by_ them (pointing at depth 2). Triage cited the stricter second reading. Worth disambiguating in the text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| transfer         | Invariant I-28                                 | Overstated. "The bytes decide an uploaded file's format" is true of the **archive signature** — `readUpload` checks `looksLikeZip` first, so a ZIP named `.json` is read as an archive — and of a name matching no extension. Among the three _text_ formats the filename wins: `formatFromFilename(name) ?? sniffTextFormat(bytes)` means a JSON document named `.csv` is parsed as CSV. The "declared format is a hint" half is stronger than the text suggests: the request's `format` field is never read by the reader at all. Recorded as `partial` rather than `stale`, since the archive clause is the one the rule exists for.                                                                                    |
| workspaces       | Testing checklist                              | Self-contradictory after the 2026-08-30 pass: still lists "After deletion, check `alarm_rules` and `entry_access` → the rows remain — the known uncovered remainder", which section 15 of the same dossier says was fixed. Also counts the server-e2e suite as "7 files"; there are 8 (`workspace-delete-residue.spec.ts` was added and not listed).                                                                                                                                                                                                                                                                                                                                                                       |
| **README.md**    | Cross-cutting findings                         | "Only `media` and `api-tokens` implement `WorkspacePurger`" is **no longer true** — `alarms`, `content/revisions` and `segments/entry-access` all ship purgers, and `workspace-delete-residue.spec.ts` asserts the whole cleanup. Fed to a triage agent as context, this claim would have produced a false `stale` verdict. The other cross-cutting findings date from the same pass and need the same re-check before anyone quotes them.                                                                                                                                                                                                                                                                                 |
| alarms           | §7 and §14                                     | State that alarms registers no `WorkspacePurger` and that a deleted workspace's rows "stay orphaned forever", listing it as an open defect. It does register one — `packages/alarms/server/src/lib/infrastructure/purge/alarms-workspace.purger.ts`, with a passing spec beside it — and `workspace-delete-residue.spec.ts:49` lists both alarm tables among those a delete clears. The Invariants section correspondingly lacks any statement about workspace deletion, which is now real, tested behaviour.                                                                                                                                                                                                              |
| alarms           | Invariant I-20                                 | The only invariant that asserts its own coverage ("pinned down by a server test and a browser test"). It happens to be true, but it means a ledger verdict was written into the dossier instead of derived from the suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| alarms           | §13 Boundaries of responsibility               | Says of `WorkspacePurgeRegistry`: "It does not register. The workspace's rules stay in the database." Stale — `AlarmsWorkspacePurger` is provided in `alarms.module.ts`, registers in `onModuleInit`, and has its own spec; the module even carries a comment explaining why it must. This is the **third** dossier carrying a version of the same dead claim, after the root README's cross-cutting findings and alarms' own §7/§14.                                                                                                                                                                                                                                                                                      |
| segments         | §17                                            | Already self-reports three of the gaps triage found (I-27, I-29, I-30), so those were known rather than new.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| shell            | Invariant I-26                                 | Says "a single shared function performs the check" for ⌘K, ⌘B and ⌘J. There are two, by necessity: ⌘K (`SidebarSearch`) and ⌘J (`CopilotLauncher`) share `isComposingText` from `utils-admin`, while ⌘B lives in the design system's `SidebarProvider`, which cannot depend on `utils-admin` and carries its own, deliberately wider `ownsBoldShortcut` (contenteditable and textual inputs, because the chord means _bold_ there). The rule is shared; the function is not.                                                                                                                                                                                                                                               |
| shell            | Testing checklist, "the sidebar and the slots" | "Take away every permission that yields a Directory row → the 'Directory' heading is not rendered at all" is unreachable against the shipped nav: `workspaces-admin` contributes its Directory row with no `permission`, so no account can empty that group. The rule (I-10) is real and now pinned by a unit fixture that can empty a group; the checklist item as written would pass in a browser for the wrong reason.                                                                                                                                                                                                                                                                                                  |
| design-system    | Invariant I-14                                 | "the rank is set by the page through `asChild`" is true of `CardTitle` and `EmptyTitle` only. `AlertTitle` has **no `asChild` prop** (`packages/design-system/src/lib/components/ui/alert.tsx`) — it has to stay the element `Alert` points `aria-labelledby` at, so a page that wants a heading nests one as a child instead. Same outcome, different mechanism; recorded `partial` rather than `stale`, since the load-bearing clause ("not headings by default") holds for all three.                                                                                                                                                                                                                                   |
| design-system    | Invariant I-30                                 | "**Every** non-operable animation is suppressed under `prefers-reduced-motion: reduce`" is currently false. `Spinner`'s `animate-spin` (`spinner.tsx:27`) carries no `motion-reduce:animate-none` and no stylesheet rule turns it off — it is the only unguarded animation in the library, and it loops for as long as the request takes. Everything else the dossier means is guarded, and the new scan in `library-conventions.spec.ts` enumerates the set with the spinner as its one named exception. Not filed as a code defect because stopping a loader outright leaves it looking frozen; what to do instead (slow it, or suppress it and lean on the `role="status"` text) is a design call.                      |
| design-system    | `Alert` source comment                         | The JSDoc beside `aria-labelledby` says "A caller that names the banner itself wins either way: `{...props}` is spread after, so an explicit `aria-label`/`aria-labelledby` overrides this one." Only `aria-labelledby` does. `aria-label` loses, because the generated `aria-labelledby` is still on the element and outranks it in the accessible-name algorithm — established by a test that failed on the comment's claim (`alert.spec.tsx` → "lets a caller point the name somewhere else").                                                                                                                                                                                                                          |
| design-system    | Header stats                                   | "15 test suites · 117 cases" is now 23 suites · 177 cases. A count in the prose has nothing pinning it, and this one has drifted twice; the same objection as `mcp` §13.4's tool counts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| design-system    | Invariant I-04                                 | "Every light-palette token has a counterpart in `.dark` — either overridden or deliberately inherited (the panel's chrome and the navigation accents)" was unfalsifiable as written *and* undercounted. Unfalsifiable because "or deliberately inherited" admits every token, so any set comparison passes by construction; undercounted because a third family inherits — the avatar palette (`--color-avatar-*`), seven identity-anchor swatches that must not change hue with the theme. **Corrected in the dossier** to name the three families explicitly, and pinned that way by `library-conventions.spec.ts`, over both `apps/admin/src/styles.css` and the scaffolder's copy. `--radius` is out of scope rather than allow-listed: it is not a palette token. |
| bootstrap        | Testing checklist, opening paragraph           | "The packages themselves have **no** `test` target of their own" — true when written, no longer. Both hosts now carry one (`packages/bootstrap/server/jest.config.js`, `packages/bootstrap/admin/vite.config.mts`), because the reasoning behind the claim only covers what a request or a browser can observe. The host's ordering and its diagnostics are neither: whether a hook ran before `NestFactory.create`, whether `trust proxy` was applied at all, which of two mounts went on first, what the layout-collision warning says. Fourteen invariants sat uncovered behind that sentence.                                                                                                                          |
| bootstrap        | Invariant I-34, second clause                  | "When the locale matches the default it is not reported at all" is observably true, but not because of the host. `react-intl` suppresses `MISSING_TRANSLATION` itself when `locale.toLowerCase() === defaultLocale.toLowerCase()` (`@formatjs/intl/lib/src/message.js:60`), so `makeIntlErrorHandler`'s own `if (locale === defaultLocale) return` is unreachable through `IntlProvider` — deleting it leaves the suite green (verified by mutation). Recorded `partial`; the guard is defensive rather than wrong, but the dossier reads as though the host enforces it.                                                                                                                                                  |
| bootstrap        | `packages/bootstrap/server/AGENTS.md`, Tests   | "`tagByResource`'s non-operation-key guard has no reachable repro: `@nestjs/swagger` emits no path-level `parameters`, and `decorate` runs *after* tagging, so nothing can stage one." True of the scanner, not of the harness — `setup-api-docs.spec.ts` has stubbed `SwaggerModule.createDocument` since the security-scheme cases were written, so the document is whatever a test says it is. Now covered (`bootstrap:I-12`), and the paragraph corrected. |
| wysiwyg          | Invariant I-15                                 | The claim ("'Clear formatting' does not remove it") is true only as of `fcd1f3b9`, and the _reason_ it gives is not the operative one. Being its own mark rather than a `textStyle` attribute is necessary and not sufficient: the toolbar's Clear formatting runs `unsetAllMarks()`, which strips **every** mark in the schema unless the mark declares `clearable: false`. The `Language` mark did not, so a WCAG 3.1.2 language marker was silently cleared along with a bold — and the extension's own docstring asserted the opposite from the same wrong premise. Reword the invariant to name `clearable: false`; the mark-vs-textStyle distinction is a separate (also true) point about `unsetMark('textStyle')`. |
| create-ortha-app | Invariant I-23                                 | Said the root `tsconfig.json` references **four** projects. It references five: the server contributes both `./apps/server` and `./apps/server/tsconfig.spec.json`, because the build project excludes the specs and without a project of their own nothing typechecks them — `template.spec.ts` has asserted that five-entry list all along. Recorded `stale` by triage; **the dossier is now corrected** (the invariant and the file tree in "The generated app") and the row is cited rather than judged.                                                                                                                                                                                                               |
| create-ortha-app | Invariant I-04                                 | "Neither `features.ts` nor any template file contains a hardcoded version number" is false as written: `package.json.tmpl` pins two dozen third-party ranges, and `config/{docs,mcp}.ts` carry the generated app's own API version. The rule that actually holds — and the one worth pinning — is that no **`@orthacms/*`** version is written down anywhere, since every one is stamped from the scaffolder's own manifest at render time. **Corrected in the dossier** and pinned that way.                                                                                                                                                                                                                              |
| create-ortha-app | Testing checklist, opening paragraph           | "five spec files" — nine after this sweep, which added the command's own end-to-end spec, the composition check, the executed `config/` modules and the packaging facts. Corrected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| create-ortha-app | Discrepancy D-07                               | Understated the blast radius: the admin half had the same defect (`webhooks` missing from `apps/admin/src/plugins.spec.ts`), so **both** halves of a freshly scaffolded app's `npm test` were red, not just the server's. Fixed, and the D-07 row now says so.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| create-ortha-app | Invariant I-15                                 | "`COPILOT_ENABLED=false` **and** `MCP_ENABLED=false` are always written into `.env`, whatever was chosen" is half false. `env.tmpl:111` sits inside the `ortha:if mcp` block (106–116), so an app scaffolded without the `mcp` feature has no `MCP_ENABLED` line at all — correctly, since it also has no `McpPlugin` and no endpoint for the setting to gate. The dossier's own §5 prose already said "MCP, **if chosen**, with `MCP_ENABLED=false`". Recorded `stale`; **the dossier is now corrected** and both halves are pinned across four wizard answers, two with `mcp` and two without. |
| copilot          | `packages/copilot/server/AGENTS.md`, "Three things that will bite you" | Opens "These are spike findings from phase 1, **each now covered by a test**". The first of the three — client-disconnect detection hanging off `res` and never `req` — had no test anywhere: `SseStream` had no spec file at all, and the copilot e2e suites contain no abort, cancel or disconnect case. It is covered as of this sweep (`sse-stream.spec.ts`, whose fixture gives the fake response a `res.req` so the broken wiring is actually reachable), which makes the sentence true rather than aspirational. Worth noting because a claim that a rule is tested is the one kind of prose that stops the next reader from checking. |
| media            | Invariant I-34 and §02 — **fixed**             | "A provider depends only on the port's **type** (erased at compile time) and its own vendor SDK; no adapter pulls in NestJS", and §02's "an adapter must be able to depend on the port **without** dragging NestJS, Drizzle and React along with it". Neither held. All six adapters imported `ObjectNotFoundError` as a *value* from `@orthacms/media-server`, whose root barrel re-exports `MediaModule` — traced with a `Module._load` hook, `require('@orthacms/media-provider-local')` reached `media-server/src/index.ts` → `lib/utils/media-plugin` → `../media.module` → `@nestjs/common` — and every adapter listed `@orthacms/media-server` in `dependencies`, so installing one installed NestJS, Drizzle, Express and Sharp. Closed by the **code**, not the text: `@orthacms/media-domain` now holds the port and its error with no dependencies of any kind, the adapters depend on that, and `adapter-packages.spec.ts` walks the whole import graph instead of scanning manifests. The dossier was then corrected to match. This is the one row here that was a code defect rather than drifted prose. |
| media            | Invariant I-33 / §14 "The one hard rule"       | Not wrong, but it was not holding: `domain/asset.ts` imported `StoredMediaTrack` from `infrastructure/schema/media-asset.ts`. Type-only, so nothing failed and nothing ever would — which is exactly how a layer rule "self-enforced" by review erodes. Fixed in `80219e9a` (the track types moved to `domain/value-objects/media-track.ts`, the schema re-exports them) and the rule is now read off the files by `domain/imports.spec.ts`, as `workspaces`, `users`, `alarms` and `transfer` already do. |
| tools            | Invariant I-25                                 | "No type and no function in the package mentions HTTP, JSON-RPC or MCP" is contradicted outright: `tool.ts:132` is `export type ToolSurface = 'mcp' | 'copilot'`, `tool-error.ts:25` is `new Logger('McpTools')`, `tool-registry.ts` signals refusals with Nest HTTP exceptions and `ToolError.status` is documented as an "HTTP-ish status". Recorded `stale`; **the dossier is now corrected** to the statement that does hold — no JSON-RPC vocabulary anywhere, and no request, response or protocol envelope crossing the contract — and pinned by `transport-neutrality.spec.ts`, which also freezes `ToolContext`'s field list, since that is the whole world a handler sees. |
| tools            | `packages/tools/server/AGENTS.md`, "Two consumers, one registry" | "That is the whole reason nothing here mentions HTTP, JSON-RPC or MCP" restates the same wrong claim as I-25 above, and is the likely source of it. The package mentions both HTTP and MCP, deliberately; what it does not do is let a transport object cross the contract. Corrected. |

## Cyrillic `И-NN` in test comments

37 comments across `packages/identity/server` and `apps/admin-e2e/src/workspaces`
cite invariants as `И-09`, `И-34` — with a **Cyrillic И**, and unqualified. They
read as citations to a human skimming, but no grep for `I-09` finds them and the
ledger never will.

They are not noise: whoever wrote those suites was tracking the invariants
already, which is why identity triaged at 30/30 and workspaces at 33/34. Worth
normalising to the ledger's `<pkg>:I-NN` form, but only against the invariant
text one by one — the numbering in the comments has not been verified to match
the dossier's, and a bulk rename would fabricate citations.
