# Design System

_Component library · packages/design-system_

**One interface vocabulary across the whole OrthaCMS admin UI**

This is **not a plugin**. The package has no routes, no NestJS module, no tables and not one permission key. `@orthacms/design-system` is a React component library built on shadcn/ui: buttons, fields, dialogs, tables, the sidebar. Its job is to make eighteen independent admin plugins, written at different times by different people, look and behave like one application, and to hand a page's author accessibility, theming and keyboard navigation _by default_ rather than as separate work.

- **49** component files
- **234** exported symbols
- **25** dependencies (16 of them Radix)
- **131** `--color-*` declarations in the palette it is styled by — which lives in the _host_, not here
- **19** consuming packages
- **0** routes, tables and permissions

## Contents

- [01. Business description](#01-business-description)
- [02. Its place in the system, and how it is consumed](#02-its-place-in-the-system-and-how-it-is-consumed)
- [03. Design tokens and theming](#03-design-tokens-and-theming)
- [04. The complete component inventory](#04-the-complete-component-inventory)
- [05. The hand-written primitives](#05-the-hand-written-primitives)
- [06. Rules and conventions for authors](#06-rules-and-conventions-for-authors)
- [07. Accessibility: what the library does on the consumer's behalf](#07-accessibility-what-the-library-does-on-the-consumers-behalf)
- [08. Working scenarios — step by step](#08-working-scenarios-step-by-step)
- [09. The package's tests](#09-the-packages-tests)
- [10. Invariants](#10-invariants)
- [11. Testing checklist](#11-testing-checklist)
- [12. Boundaries of responsibility](#12-boundaries-of-responsibility)
- [13. Where the code and the documentation diverge](#13-where-the-code-and-the-documentation-diverge)

## 01. Business description

The OrthaCMS admin UI is assembled from plugins: content, media, workspaces, members, the copilot, audiences, checks, analytics — each lives in its own package and is developed separately. Without a shared library that is eighteen different ideas of what a “button”, an “empty state” and a “modal window” are. The Design System is the one vocabulary that makes them a single product.

### The problem it solves

- **Visual coherence with no coordination.** The author of a page in `content/admin` does not agree a corner radius or a border shade with the author of a page in `media/admin` — both take `Card` and `Button`. Every styling decision was made once and lives in one place.
- **Accessibility by default rather than from memory.** This is the biggest saving. Associating a label with a field, `role="alert"` on an error, `scope="col"` on a table header, a dialog's focus trap, keyboard reachability of a scrollable region — all of it is already inside the primitive. A plugin that simply took `InputField` gets a correct form knowing nothing about WCAG.
- **Theming by one switch.** The light and dark themes are two sets of CSS variables. The components do not know which theme is active: they are written against semantic classes (`bg-card`, `text-muted-foreground`) rather than specific colours. Switching the theme requires an edit in not one component.
- **One place to fix a defect.** The pointer cursor on buttons, a visible focus ring in high-contrast mode, suppressing animation under `prefers-reduced-motion` — each of those fixes was made once in the shared layer and propagated instantly to every page. A fix “at each call site” would have been incomplete, guaranteed.
- **Cheap extension.** Components are added by the shadcn CLI straight into the package's sources — that is a _copy of the code_, not a dependency on somebody else's version. So any component can be adapted to the product, and no upstream release will break it.

### Who sees it

#### The admin-plugin author

Writes `import { Button, Card, cn } from '@orthacms/design-system'` and gets a finished page of the right shape. Introduces no styles of their own, does not pull in Radix directly, does not think about the dark theme.

#### The admin-UI user

Sees one product rather than a set of mismatched screens. Can choose a light or a dark theme, work by keyboard alone, turn on high contrast or turn off animation — the interface survives all of it.

#### The owner of a generated application

Gets the library in the `CORE_PACKAGES` set of the `create-ortha-app` template — it is installed unconditionally, because it is the first thing you reach for when writing a page of your own.

### What the Design System is not

The boundaries explain why the package lacks things a “design system” is often expected to have:

- **It is not a plugin.** Neither an `AdminPlugin` nor a `ServerPlugin`. The package registers nothing, adds no routes and takes no part in the application's lifecycle. It is simply imported.
- **It is not where the theme lives.** The tokens (`@theme`, `.dark`, the avatar palette) are declared in `apps/admin/src/styles.css` — in the host application, not in the package. The package only _consumes_ them through semantic Tailwind classes. The library's own CSS is one small file, for rules that cannot be expressed as utilities.
- **It is not an i18n layer.** The package has no `react-intl` and no translation catalogue. Every visible string comes from the consumer; the exception is pure iconography (the close cross, the sidebar toggle), whose default English text the host overrides through one context.
- **It is not a form library.** `InputField` is presentational and form-engine-agnostic: it takes native `<input>` props and a list of errors. Validation and form state belong to the consumer (in the admin UI, TanStack Form).
- **It is not a Storybook or a showcase.** There is no component catalogue with examples in the repository; the documentation is the JSDoc above each export and the real call sites.
- **It is not a charting library.** The `--color-chart-*` tokens are declared in the host and used by `insights/admin`'s widgets; the visualisation components themselves do not live in the design system.

> **The architecture's key idea**
>
> shadcn/ui is **not an npm dependency but a source generator**. The CLI puts a component's code into `src/lib/components/ui/`, and from that moment it is ours: it can be rewritten, its role renamed, a prop added. That is why half the files in the inventory diverge from upstream — `Alert` stopped being a heading, `Spinner` stopped being a live region, `Table` learned to be a keyboard-reachable scroll region. The flip side: upstream updates do not arrive by themselves, and each has to be merged by hand.

## 02. Its place in the system, and how it is consumed

The package is **flat** (`packages/design-system`, with no `admin`/`server` split), because there is nothing to split: it has no server half and cannot have one. It is published as `@orthacms/design-system`, version `0.4.2` — the same as every other package in the monorepo (releases go out in lockstep, as one set).

### What it consists of

| Path                         | What is there                                                                                                        | Size        |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------- | ----------- |
| src/index.ts                 | The one public surface — a barrel of re-exports. Anything not in it does not exist for a consumer                    | 234 symbols |
| src/lib/components/ui/       | The components. One file is one family (`dialog.tsx` yields ten exports)                                             | 49 files    |
| src/lib/utils.ts             | The `cn` utility — joining Tailwind classes with conflict resolution                                                 | 7 lines     |
| src/lib/appearance/          | `AppearanceProvider` / `useAppearance` — theme choice, synchronised with `<html>` and `localStorage`                 | 1 module    |
| src/lib/labels/              | `DesignSystemLabelsProvider` — the one localisation seam for the library's own “furniture”                           | 1 module    |
| src/lib/hooks/use-mobile.tsx | `useIsMobile` — the single layout breakpoint (768px)                                                                 | 1 hook      |
| src/styles.css               | The global sheet: dropdown and wizard-step animations, the cursor on interactive elements, the `forced-colors` block | ~190 lines  |
| src/test-setup.ts            | jsdom shims for APIs it lacks: `matchMedia`, `ResizeObserver`, pointer capture, `scrollIntoView`                     | 1 file      |
| components.json              | The shadcn CLI's configuration: the `new-york` style, the `neutral` base colour, `lucide` icons, CSS variables on    | —           |

### How a component reaches the browser: there is no build

In the monorepo packages are consumed **from source**. For `design-system`, `main`, `types` and all three branches of `exports["."]` point at the same `./src/index.ts`; the `./styles.css` subpath is published separately. No build step is needed before consumption — the `apps/admin` application's Vite transpiles the design system's TSX directly, like its own code.

Two practical facts follow, and they are the ones that most often bite a newcomer:

1. **Vite does not typecheck.** A type error in a library component will not stop the dev server. It is caught by a separate `admin:dev:typecheck` task (`tsc --build --watch`) — the fourth pane in `npm run dev`.
   _the same is true of any admin plugin_
2. **The `@/` alias does not resolve inside the library.** It is declared in `tsconfig.lib.json` (`"@/*": ["./src/*"]`) and works for `tsc`, but the consuming application's Vite dev server does not see it and fails with `Failed to resolve import "@/lib/utils"`. Hence the first of the two mandatory rules (section 6).

The tarball published to npm is built differently: `tools/release/pack.mjs` compiles the package through `tsconfig.lib.json` and assembles, under the temporary root `dist/pack/<projectRoot>/`, a rewritten manifest whose `exports` already point at `./dist`. `tsc` does not emit `src/styles.css`, so `pack` copies it separately (every non-TS asset next to the sources is carried to the same relative position).

### What the host must mount

The library does not work “on its own” — four of its mechanisms need context, and the host mounts all four once in `createAdmin` (`packages/bootstrap/admin`):

| Provider           | Why                                                                                                                                                                  | Where in the tree                                                                  |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| AppearanceProvider | The theme. It holds the choice and reflects it as a `dark` class on `<html>` and through `color-scheme`                                                              | At the very top, above the router — so every route and every toast reads one theme |
| DesignSystemLabels | Localising the “furniture”. The host's wrapper takes a string from `react-intl` and puts it into `DesignSystemLabelsProvider`                                        | Inside `IntlProvider`, above all the rest of the UI                                |
| TooltipProvider    | A shared tooltip delay (`delayDuration={200}`)                                                                                                                       | Under `DesignSystemLabels`                                                         |
| Toaster            | The toast host. Mounted **outside** `AppErrorBoundary` — a toast is how the application reports trouble, and it has to survive the very trouble the boundary catches | As the last child of `TooltipProvider`                                             |

Separately, the styles: `apps/admin/src/styles.css` imports `tailwindcss` first, then `packages/design-system/src/styles.css`, and declares an `@source` glob over the library's sources so Tailwind scans its classes.

### Who consumes it, and how heavily

An import from `@orthacms/design-system` appears in **352 files** across eighteen admin packages; **166** distinct exported names are in everyday use.

| Package                                                                        | Files              | What it most characteristically takes                                                                                    |
| ------------------------------------------------------------------------------ | ------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| content/admin                                                                  | 67                 | `Button`×26, `Spinner`×17, `cn`×14, `Badge`×13 — the largest and densest surface in the admin UI                         |
| workspaces/admin                                                               | 45                 | `InputGroup`×6, `Avatar`+`avatarColorVar`, `Sidebar*`, `StatTile` — the only consumer of the metric tile                 |
| users/admin                                                                    | 42                 | `Alert`+`AlertDescription`×13, `Card*`, `RadioGroup`, `useAppearance`×2 (the theme settings screen)                      |
| media/admin                                                                    | 31                 | `Dialog`×5, `Drawer`, `Progress`×2 (the only consumer of the progress bar — file uploads)                                |
| copilot/admin                                                                  | 25                 | `Sheet` (its only consumer — the docked panel), `Kbd`, `TopBarActions`                                                   |
| wysiwyg/admin                                                                  | 23                 | All of `DropdownMenu*`, the `Sub`/`RadioGroup` variants included — the editor's toolbar                                  |
| identity/admin                                                                 | 22                 | `Card*`×13, `Field*`, `AppLoader` (its only consumer), `Logo`                                                            |
| alarms/admin, segments/admin, insights/admin, activity/admin                   | 14 / 13 / 12 / 12  | `Alert`, `Skeleton`, `Table*`, `Pagination`, `SearchToolbar`                                                             |
| shell/admin                                                                    | 11                 | The only consumer of `Sidebar`, `SidebarProvider`, `SidebarInset`, `useSidebar`, `useIsMobile` — the application's frame |
| query-builder/admin, i18n/admin, api-tokens/admin, transfer/admin, utils/admin | 11 / 8 / 7 / 3 / 2 | `SegmentedControl`, `Select`, `MultiSelect`, `Drawer`, `AVATAR_COLORS`                                                   |
| bootstrap/admin                                                                | 4                  | Draws no pages — it mounts the four providers from the table above                                                       |

### Dependencies

24 ordinary dependencies and 2 peers (`react` and `react-dom`, both `^19.0.0`). Fifteen of the twenty-four are Radix primitives, that is, the bulk of the behaviour (focus traps, roles, roving focus, portals) comes from there, and the library adds the styling and the product decisions on top.

| Dependency                      | What it provides                                                                                                                                  |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| @radix-ui/react-\* (15 of them) | avatar, checkbox, collapsible, dialog, dropdown-menu, label, popover, progress, radio-group, select, separator, slot, tabs, toggle-group, tooltip |
| class-variance-authority        | Typed variants (`buttonVariants`, `badgeVariants`, `alertVariants`, `fieldVariants`, `emptyMediaVariants`, `sidebarMenuButtonVariants`)           |
| clsx + tailwind-merge           | The two halves of the `cn` utility                                                                                                                |
| cmdk                            | The command palette and the search inside `MultiSelect`                                                                                           |
| sonner                          | Toasts. `toast` is re-exported straight from the barrel                                                                                           |
| vaul                            | `Drawer` — a panel sliding in from the bottom or the side                                                                                         |
| react-day-picker + date-fns     | `Calendar`, `DatePicker`, `DateTimePicker`                                                                                                        |
| lucide-react                    | The icons. The one icon library, pinned in `components.json`                                                                                      |

> **What is not among the dependencies — and it shows**
>
> `tailwindcss-animate` / `tw-animate-css` are **installed nowhere in the repository**. Which means the `animate-in`, `fade-in-0`, `zoom-in-95` classes shadcn puts on overlays **generate no CSS at all**. The animation of the dropdown layers (`Popover`, `DropdownMenu`, `Select`) is hand-written in `src/styles.css` as the `.ds-dropdown-motion` class. `dialog.tsx`, `sheet.tsx` and `tooltip.tsx`, meanwhile, still carry those very dead classes — harmless, but misleading.

## 03. Design tokens and theming

Not one component in the library contains a specific colour. They are all written against semantic Tailwind utilities — `bg-background`, `text-muted-foreground`, `border-input`, `bg-success-soft` — which expand into CSS variables. The variables themselves are declared **not in the package** but in the host application: `apps/admin/src/styles.css`. That is a deliberate split: the library describes colour _roles_, the application their _values_.

| Block in `apps/admin/src/styles.css` | What it declares                                                                                                                                                                                                                               | Variables |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| @theme { … }                         | The whole light palette plus `--radius`. Specifically `@theme` rather than `@theme inline` — so that utilities expand into `var(--color-*)` and the dark theme's override takes effect **at runtime** rather than being baked in at build time | 70        |
| .dark { … }                          | The dark palette — a redeclaration of the same semantic tokens. The sidebar tokens and the navigation accents are not in it: they are written dark to begin with and carry over unchanged                                                      | 55        |
| :root { … }                          | The avatar palette, 7 shades. Declared as ordinary properties rather than in `@theme`: they are substituted inline through `var(--color-avatar-…)`, and Tailwind prunes from `@theme` any token no generated utility referenced                | 7         |
| @custom-variant dark                 | `(&:where(.dark, .dark *))` — how the `dark:` variant is spelled                                                                                                                                                                               | —         |

### Token families

| Family                                                                    | Role                                                    | What is special about it                                                                                                                                                                                       |
| ------------------------------------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| background / foreground / card / popover                                  | The page's canvas and the surfaces above it             | In the light theme a card matches the background and is set apart by a border and a shadow; in the dark theme a card is a step lighter than the canvas, so it is set apart by the surface too                  |
| primary / primary-foreground                                              | The main (“contrast”) button — ink                      | Inverted in the dark theme: a light fill with dark text                                                                                                                                                        |
| brand / brand-text / brand-soft                                           | The one bright accent — flame orange                    | **White text on `bg-brand` does not pass AA.** There is a separate `brand-text` for link text and the ink `brand-foreground` for fills. Orange is the colour of icons and accents, not a bed for white letters |
| secondary / muted / accent                                                | Muted surfaces, row hover, chips                        | `secondary` is a step darker than `muted`/`accent` so secondary chips do not dissolve into a highlighted row                                                                                                   |
| destructive / success / warning / info                                    | Four statuses, each as a pair: solid + soft             | The soft half is a tinted surface with dark text in the same tone; used by `Badge`, `Alert` and typed toasts                                                                                                   |
| teal-soft / violet-soft                                                   | Soft pairs with no solid twin                           | The icon tiles in the top bar for areas with a teal/violet accent (Media, Workspaces, Settings)                                                                                                                |
| border / input / ring                                                     | The structural border, the input border, the focus ring | `input` is deliberately darker than `border` — so a field reads as editable without darkening the card dividers                                                                                                |
| sidebar-\*                                                                | Eight tokens of the sidebar's dark chrome               | Everything drawn inside the panel must use `sidebar-*` rather than the light surface tokens                                                                                                                    |
| nav-orange … nav-teal                                                     | Six navigation-icon accents                             | Lightened to L ≥ 0.72 so each gives 3:1 against the dark panel                                                                                                                                                 |
| status-active / status-invited                                            | Status dots (active / invited)                          | Always paired with a text label — meaning is never carried by colour alone                                                                                                                                     |
| chart-1 / chart-2 / chart-q0…q5 / chart-on-deep / chart-grid / chart-axis | The visualisation palette for the Insights widgets      | The only tokens in **hex** rather than oklch: they are a contrast validator's output, and moving to another colour space would drift the rounding away from the checked numbers                                |
| avatar-slate … avatar-indigo                                              | Seven initial accents                                   | White initials on each give ≥ 5.7:1                                                                                                                                                                            |

### How the theme is switched

1. **The initial state is read synchronously.** `useState(readStoredTheme)` — the value from `localStorage['ortha.theme']` is taken right in the initialiser, so the first paint is already right and there is no flash of the wrong theme.
   _unavailable or corrupt storage quietly yields the system default_
2. **`system` is resolved against the OS.** The subscription to `matchMedia('(prefers-color-scheme: dark)')` lives for the provider's whole lifetime, so a change of system theme applies without a reload.
3. **The result is reflected on the document.** `applyToDocument` sets or clears the `dark` class on `<html>` and sets `style.colorScheme`, so the browser repaints its own furniture — scrollbars, native controls.
4. **The choice is stored.** The `localStorage` write is wrapped in a `try/catch`: a private window or an exhausted quota loses only the cross-session memory, not the theme change itself.
5. **The durable copy is on the server.** The provider knows nothing about it: a plugin reading the user's profile simply calls `setTheme`. `setTheme` compares the value with the current one and does not touch state when it has not changed — and hydration from the server usually repeats the value already in force.
6. **The hook does not fail outside a provider.** `useAppearance` without an `AppearanceProvider` returns `{ theme: 'system', resolvedTheme: 'light', setTheme: () => undefined }` — a component in isolation (a test) still renders.

`Toaster` is the one component that reads the resolved theme explicitly: it passes `resolvedTheme` to sonner, because sonner has chrome of its own that would otherwise drift away from the application.

### What the library's own CSS takes on

`src/styles.css` is the only file a consumer imports besides the barrel, and it is not about theming: it is about what cannot be expressed as a utility.

| Block                           | What it solves                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| .ds-dropdown-motion             | The appearance and disappearance of `Popover`, `DropdownMenu`, `Select`. The `--ds-drop-x/y` offset depends on `data-side`, so the layer “slides out” from its trigger's side. Leaving is faster than arriving (90 ms against 140): closing must feel instant, opening deliberate. It works because Radix keeps the node mounted while the `[data-state=closed]` animation runs |
| .wizard-step-in                 | A wizard step appearing. **Only the transform** is animated, opacity is always 1 — so the content is not invisible when animation clocks are stopped (a background tab, printing)                                                                                                                                                                                               |
| @layer base — the cursor        | Tailwind v4 no longer puts `cursor: pointer` on buttons. Restored here for every operable control; disabled ones explicitly get `not-allowed` (all three ways of disabling are covered: `:disabled`, `aria-disabled`, `data-disabled`)                                                                                                                                          |
| @media (forced-colors: active)  | The most important one. See the box below                                                                                                                                                                                                                                                                                                                                       |
| .ds-spinner                     | `Spinner`'s rotation. Owns **both** branches — a 1 s rotation normally, and under `prefers-reduced-motion` a 1.6 s opacity breath with no `transform` at all. Not `animate-spin` + `motion-reduce:animate-none`, because a stopped loader reads as a frozen app and there is no Tailwind utility for the replacement                                                            |
| @media (prefers-reduced-motion) | The dropdown and wizard animations are switched off; the spinner's is **replaced** rather than removed (see above). Duplicated on `Skeleton` through the `motion-reduce:animate-none` utility                                                                                                                                                                                   |

> **Forced-colors mode is not cosmetics**
>
> Under `forced-colors: active` (Windows High Contrast) the browser **discards `box-shadow` entirely**. And every focus indicator in this library is a Tailwind `ring-*` utility, that is, a `box-shadow`. The consequence: a keyboard user in high contrast **could see focus nowhere in the whole admin UI** — a 2.4.7 and 1.4.11 failure across every page at once, which **no axe run sees** (axe does not emulate that media feature). The cure is a real `outline`, which the mode preserves. The `!important` here is load-bearing rather than lazy: several components carry a `focus-visible:outline-none` next to their ring, and by specificity a class with a pseudo-class beats a bare `:focus-visible` — without `!important` the outline would be switched off by exactly the components that need it most. Separately: a selected state drawn as a plain fill (`[role=option][aria-checked=true]`, an active panel item, `[aria-current]`) gets a `Highlight` border, and disabled controls get `GrayText`, because `opacity` is not part of the forced palette and a disabled control would read as a working one. `forced-color-adjust: none` is deliberately used nowhere — that is precisely the failure this block was written to prevent.

## 04. The complete component inventory

48 files, 233 exported symbols. Below is everything `src/index.ts` hands out, grouped by purpose. The “Most used” column carries the actual import counts across the repository (files in which the name is imported); it shows what in the library is load-bearing and what is nearly museum-piece.

> **How to read the “Accessibility” column**
>
> It records what the component does **on the consumer's behalf** — roles, associations, focus order. Where a cell says “requires from the caller”, that is what the library cannot provide and what you will have to pass yourself.

### 4.1 Forms and input

| Component                                                                               | Purpose                                                                                                                                                           | Key props / variants                                                                                                                              | Accessibility                                                                                                                                                                                                                                                                                                      | Most used |
| --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- |
| Input                                                                                   | A single-line field. Every native `<input>` prop passes straight through                                                                                          | No variants; size and shape come from `className`. Height `h-9`, background `bg-card`, border `border-input`                                      | The styling reaction to `aria-invalid` is built in (`aria-invalid:border-destructive`) — but the attribute itself is set by the caller. It gives no label: a bare `Input` without a `<label>` is a defect                                                                                                          | 23        |
| InputField                                                                              | **A complete composite form field**: the label (with an optional action on the same line), the field, the hint, the error. Assembled from the `Field*` primitives | `id` (required), `label`, `description`, `error`, `errors`, `invalid`, `labelAction`, `fieldClassName`; everything else goes to the `<input>`     | Does all of it: `htmlFor`↔`id`, `aria-invalid`, and an `aria-describedby` assembled from the hint, the error and any user-supplied value. **`invalid={false}` does not suppress the message** — it is the only thing that says what to fix                                                                        | 14        |
| Textarea                                                                                | A multi-line field                                                                                                                                                | As `Input`; `min-h-[60px]`                                                                                                                        | The same as `Input`: it reacts to `aria-invalid` and supplies no label                                                                                                                                                                                                                                             | 7         |
| Label                                                                                   | A label built on Radix `Label`                                                                                                                                    | `className` only. Reacts to `peer-disabled`                                                                                                       | Radix provides click-the-label → focus-the-control                                                                                                                                                                                                                                                                 | 16        |
| Field                                                                                   | One field's wrapper — the basic building block of forms                                                                                                           | `orientation`: `vertical` (the default) / `horizontal` / `responsive` (which flips at the `@md` container query)                                  | `role="group"`, and `data-invalid` paints the whole group in the error colour. A checkbox or radio in the horizontal layout aligns to the text's first line                                                                                                                                                        | 15        |
| FieldLabel                                                                              | A label inside a `Field`. It can be a “choice card”: with a `Field` nested inside it, the label turns into a frame that highlights on `data-state=checked`        | `Label`'s props; `htmlFor` is required                                                                                                            | Dims along with a disabled field through `group-data-[disabled=true]/field`                                                                                                                                                                                                                                        | 10        |
| FieldDescription                                                                        | A hint under the control                                                                                                                                          | `id` — so the caller can wire it up through `aria-describedby`                                                                                    | Links inside are underlined and change colour on hover — not by colour alone                                                                                                                                                                                                                                       | 12        |
| FieldError                                                                              | An error message                                                                                                                                                  | `children` (one message) **or** `errors`: an array of `{ message?: string }`. With more than one message a `<ul>` is drawn                        | `role="alert"`. **Returns `null` when there is nothing to say**: `errors={[]}` and `errors={[{}, undefined]}` are truthy as values, and used to open an empty live region that announced nothing                                                                                                                   | 8         |
| FieldGroup                                                                              | A vertical stack of fields. The `@container/field-group` query container — the `responsive` variant depends on it                                                 | `className`                                                                                                                                       | Carries no semantics of its own; the meaning of the grouping comes from `FieldSet`                                                                                                                                                                                                                                 | 6         |
| FieldSet · FieldLegend                                                                  | Real `<fieldset>` and `<legend>` for related checkboxes/radios                                                                                                    | `FieldLegend variant`: `legend` (larger) / `label`                                                                                                | Native grouping semantics — which is why replacing them with a `div` plus a heading is forbidden                                                                                                                                                                                                                   | —         |
| FieldContent · FieldTitle · FieldSeparator                                              | A column of text beside a control; a heading line with no label semantics; a separator that can carry a caption in its middle                                     | `FieldSeparator` takes `children` — the text on the line                                                                                          | `FieldTitle` is deliberately not a `<label>`: it is for captions that do not belong to a single control                                                                                                                                                                                                            | 1         |
| InputGroup<br>+ Addon, Button, Text, Input, Textarea                                    | A field with fused elements: an icon inside, a button in the corner, a prefix/suffix                                                                              | **Only** `InputGroupInput`/`InputGroupTextarea` go inside, never a bare `Input`/`Textarea`                                                        | `role="group"` on the wrapper and on the addon; an error state is raised to the whole group through `has-[[aria-invalid=true]]`                                                                                                                                                                                    | 9         |
| Checkbox                                                                                | A Radix checkbox                                                                                                                                                  | Radix's props (`checked`, `onCheckedChange`, `disabled`)                                                                                          | Radix provides `role="checkbox"`, `aria-checked` and space-bar operation. The label is associated by the caller                                                                                                                                                                                                    | 17        |
| RadioGroup · RadioGroupItem                                                             | A mutually exclusive choice                                                                                                                                       | `value`, `onValueChange`, `orientation`                                                                                                           | Radix: `radiogroup`, roving focus by arrow keys, one tab stop for the whole group                                                                                                                                                                                                                                  | 7         |
| Select<br>+ Group, Value, Trigger, Content, Label, Item, Separator, ScrollUp/DownButton | A single-choice dropdown                                                                                                                                          | Radix's props; the trigger reacts to `aria-invalid`, and the content is bounded by `--radix-select-content-available-height`                      | Radix provides the roles, type-ahead and focus return. The content carries `.ds-dropdown-motion`. The control's name comes from the caller (a `FieldLabel` or an `aria-label`)                                                                                                                                     | 15        |
| MultiSelect                                                                             | **A multiple choice with search.** shadcn has no such component — this one is assembled from `Popover` + `Command` + `Badge`                                      | `options`, `value`, `onChange`, `placeholder`, `searchPlaceholder`, `emptyText`, `id`, `invalid`, `disabled`, `aria-describedby`, **`container`** | The trigger is declared `role="combobox"` + **`aria-haspopup="listbox"`** (Radix says `dialog` by default, while what opens is a listbox). Selectedness rides on `aria-checked`, because cmdk has taken `aria-selected` for its highlight. The search matches both `label` and `value`                             | 2         |
| Calendar · CalendarDayButton                                                            | A calendar grid on `react-day-picker` v10                                                                                                                         | The cell size is set by the `--cell-size` variable                                                                                                | Navigation and markup come from `react-day-picker`; disabled days through `aria-disabled`                                                                                                                                                                                                                          | —         |
| DatePicker                                                                              | A date field: a trigger showing the formatted date opens a popover with a calendar                                                                                | `value: Date \| undefined`, `onChange`, `placeholder`, `id`, `invalid`, `disabled`, `onBlur`, `aria-describedby`                                  | The popover is as wide as the trigger but no wider than 300px. No i18n: the format and the captions come from the caller                                                                                                                                                                                           | 1         |
| DateTimePicker                                                                          | One field for a date and a time: a popover of two sections — the calendar and, below a separator, a time field                                                    | The same plus `timeLabel`                                                                                                                         | **Seconds are preserved rather than discarded**: the time field runs with `step="1"`, and a round trip through `HH:mm` used to erase what had been typed — on the very next render and on every change of day                                                                                                      | 1         |
| SegmentedControl<br>+ Item, Count                                                       | A segmented control — a small set of mutually exclusive options (AND/OR in the filter builder, for instance)                                                      | A wrapper over Radix `ToggleGroup` with `type="single"` fixed; `value`, `onValueChange`, `defaultValue`                                           | Radix provides arrow-key navigation and the semantics. **The active segment is marked by more than colour**: a `primary` fill plus a `font-medium → font-semibold` shift. `Count` inverts its background to stay readable on the fill                                                                              | 8         |
| SearchToolbar                                                                           | The standard toolbar of a list page: a search field on the left, a cluster of actions on the right                                                                | `value`, `onValueChange`, `searchLabel`, `searchPlaceholder`, `actions`, `busy`, **`inputRef`**                                                   | The field is named by `aria-label={searchLabel}`. `busy` swaps the magnifier for a spinner — **purely visually**; announcing the load stays with the list. `inputRef` exists for 2.4.3: the “clear filters” button lives inside the empty state it destroys, and without a focus move focus would land on `<body>` | 3         |

### 4.2 Actions, statuses and feedback

| Component                                             | Purpose                                                                                                | Key props / variants                                                                                                                                                                                                           | Accessibility                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Most used                               |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| Button<br>+ buttonVariants                            | **The library's most-used component.** A real `<button>`, or any element through `asChild`             | `variant`: `default` (an ink fill) · `destructive` · `outline` · `secondary` · `ghost` · `link` (which uses `brand-text` — orange at link level). `size`: `default` (h-9) · `sm` (h-8) · `lg` (h-10) · `icon` (9×9). `asChild` | A native button: Enter/Space, tab order, `disabled`. Focus is `focus-visible:ring-2 ring-ring`. Icons inside are automatically `size-4` and `pointer-events-none`. **There is no loading state of its own** — it is assembled from `Spinner` + `disabled`                                                                                                                                                                                                               | 146 (content ×26, users ×20, media ×19) |
| Badge<br>+ badgeVariants                              | A status or label chip                                                                                 | Nine variants: `default` · `secondary` · `destructive` · `outline` plus five “soft” ones: `success` · `warning` · `info` · `destructive-soft` · `primary-soft`                                                                 | `gap-1` was moved into the base deliberately: a chip with an icon is no rarer than a text one, and everyone who forgot the gap got a glyph glued to the first letter                                                                                                                                                                                                                                                                                                    | 43                                      |
| Alert · AlertTitle · AlertDescription                 | A page-level banner                                                                                    | `variant`: `default` · `destructive` · `success` · `warning` · `info` (the last four on soft surfaces, matching the `Badge` variants)                                                                                          | `role="alert"`, named through an `aria-labelledby` pointing at `AlertTitle`. **`AlertTitle` is a `<div>`, not a heading**: a heading's rank depends on where the banner is mounted, and the component cannot know that; it used to be a hard-coded `<h5>`, so any banner under an `<h1>` skipped four levels. The attribute is written **only when the title actually mounted** — otherwise it would be a reference into nothing, which is worse than an unnamed banner | 41                                      |
| Toaster · toast                                       | The notification host and the function that raises them. `toast` is re-exported straight from `sonner` | The position is fixed — the **bottom right corner** — with the close button on. Typed toasts (`toast.success/.error/.warning/.info`) ride on soft semantic surfaces                                                            | sonner's live region announces new toasts. **Clicking a toast's body dismisses that toast**: sonner sets no id in the DOM, and `toast.dismiss()` with no argument clears the whole stack — so the handler finds the toast's own close button and presses it. Clicks on interactive children (the action, the cancel, links) are not intercepted                                                                                                                         | 43                                      |
| ConfirmDialog                                         | A small modal confirmation of a dangerous action                                                       | `open`, `onOpenChange`, `title`, `description`, `confirmLabel`, `cancelLabel`, `confirmVariant`, `busy`, **`onCloseAutoFocus`**, `onConfirm`                                                                                   | While `busy`, the dialog does not close and both buttons are disabled — a double submit is impossible. `onCloseAutoFocus` exists for the main case: a confirmed **deletion** usually destroys the very button that opened the dialog, and focus restoration aims at a detached node                                                                                                                                                                                     | 20                                      |
| Spinner                                               | An indeterminate loading indicator                                                                     | `<svg>` props; size and colour through `className` and `currentColor`                                                                                                                                                          | **Decorative by default (`aria-hidden`)** — a deliberate reversal. It used to carry `role="status"` on the `<svg>` itself with no text inside: a live region with no content announces nothing, while four spinners on a page created four competing regions. Announcing busyness should be done by something that has words. Under `prefers-reduced-motion` it stops rotating and breathes instead — opacity only, never stopped outright                              | 51                                      |
| Skeleton                                              | One pulsing placeholder rectangle                                                                      | The shape comes from `className`                                                                                                                                                                                               | Carries no text and is ignored by screen readers. **The pulse is suppressed under `prefers-reduced-motion`** — an animation that runs for as long as the request does is exactly what a motion-sensitive user asked not to be shown                                                                                                                                                                                                                                     | 34                                      |
| SkeletonRegion                                        | An announcing wrapper around a block of `Skeleton`s                                                    | `label` (required, `sr-only`), `heading` (an optional `<h1>` for a **route-level** skeleton)                                                                                                                                   | One `role="status"` + `aria-busy`; the placeholders inside are hidden with `aria-hidden`. **One region per loading surface.** The inner wrapper uses `display: contents` so as not to break the caller's `space-y-*`/`grid`. This used to exist only as advice in a JSDoc — and a page going into a load announced nothing                                                                                                                                              | 3                                       |
| Empty<br>+ Header, Media, Title, Description, Content | A list's or a page's empty state                                                                       | `EmptyMedia variant`: `default` / `icon` (a 40×40 tile with an icon). `EmptyTitle asChild`                                                                                                                                     | `EmptyTitle` is a `<div>` by default, because most often an empty state lives inside a page that already has a heading. Where the empty state **is** the page — `asChild` and an `<h1>` of your own                                                                                                                                                                                                                                                                     | 17                                      |
| Progress                                              | A determinate indicator                                                                                | `value` — 0…100                                                                                                                                                                                                                | Radix provides `role="progressbar"` and `aria-valuenow`. **It requires from the caller** an `aria-label`/`aria-labelledby` naming the process, and an `aria-valuetext` where a bare percentage reads badly (“3 of 5 files”)                                                                                                                                                                                                                                             | 2                                       |
| Tooltip<br>+ Provider, Trigger, Content               | A hover tooltip                                                                                        | `sideOffset` (4 by default); the delay is set once on `TooltipProvider`                                                                                                                                                        | Radix: shown on hover **and on focus**, closed by Escape. A tooltip does not stand in for a control's name                                                                                                                                                                                                                                                                                                                                                              | 8                                       |
| Kbd                                                   | A key chip (`↵`, `esc`, `⌘K`)                                                                          | A real `<kbd>`; `pointer-events-none`                                                                                                                                                                                          | Styled for light surfaces — inside the dark sidebar it needs overriding to the `sidebar-*` tokens                                                                                                                                                                                                                                                                                                                                                                       | 3                                       |
| Separator                                             | A divider                                                                                              | `orientation`, `decorative` (`true` by default)                                                                                                                                                                                | `decorative` removes the element from the accessibility tree — the right behaviour for a purely visual line; a meaningful divider passes `decorative={false}`                                                                                                                                                                                                                                                                                                           | 7                                       |

### 4.3 Overlays

| Component                                                                                                                                             | Purpose                                                                    | Key props / variants                                                                                                        | Accessibility                                                                                                                                                                                                                                                                                             | Most used                                                |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| Dialog<br>+ Portal, Overlay, Trigger, Close, Content, Header, Footer, Title, Description                                                              | A modal window                                                             | `DialogContent` adds exactly one prop of its own to Radix's — **`closeLabel`**                                              | Radix: the focus trap, focus return to the trigger, Escape, and `aria-labelledby`/`aria-describedby` from `Title`/`Description`. `DialogTitle` is mandatory (it may be `sr-only`). The close cross's default name comes from `DesignSystemLabelsProvider` rather than from an English literal on the spot | 23                                                       |
| Sheet<br>+ Portal, Overlay, Trigger, Close, Content, Header, Footer, Title, Description                                                               | A sliding panel on Radix Dialog                                            | `side`: `right` (the default) / `left` / `top` / `bottom`; `closeLabel`                                                     | The same machinery as `Dialog`. Also used inside `Sidebar` as the panel's mobile variant                                                                                                                                                                                                                  | 1 (copilot)                                              |
| Drawer<br>+ Portal, Overlay, Trigger, Close, Content, Header, Footer, Title, Description                                                              | A drawer with a drag gesture, on `vaul`                                    | vaul's props (`direction`, `shouldScaleBackground`)                                                                         | Its own focus capture from vaul. Used for filters on a narrow screen and for the media panel                                                                                                                                                                                                              | 3                                                        |
| Popover<br>+ Trigger, Content, Anchor                                                                                                                 | A non-modal floating layer                                                 | Radix's props plus one of its own, **`container`**: where to portal the content                                             | `container` is not cosmetics: `react-remove-scroll` permits scrolling only within the blocking ancestor's subtree, and a popover portalled into `document.body` from inside a dialog stops scrolling with the mouse wheel. Carries `.ds-dropdown-motion`                                                  | 12                                                       |
| DropdownMenu<br>+ Trigger, Content, Item, CheckboxItem, RadioItem, RadioGroup, Label, Separator, Shortcut, Group, Portal, Sub, SubTrigger, SubContent | A context menu — 15 exports, the fullest family after `Sidebar`            | Radix's props; nested menus through `Sub*`                                                                                  | Radix: `role="menu"`, roving focus, first-letter type-ahead, Escape, focus return. A kebab trigger in a table row **requires from the caller** a name tied to the row (“Actions for {name}”)                                                                                                              | 20 (wysiwyg is the only one taking Sub\* and RadioGroup) |
| Command<br>+ Dialog, Input, List, Empty, Group, Item, Separator, Shortcut                                                                             | The command palette on `cmdk`; also the search engine inside `MultiSelect` | `CommandDialog` takes a `title` and a `description` — English “Command palette” / “Search for a command to run.” by default | `CommandDialog` always draws a `DialogTitle`+`DialogDescription` inside an `sr-only` header — without which Radix complains and the dialog stays unnamed                                                                                                                                                  | 4                                                        |
| Collapsible<br>+ Trigger, Content                                                                                                                     | A collapsible block                                                        | A re-export of Radix's primitives with nothing added                                                                        | Radix: `aria-expanded`, `aria-controls`                                                                                                                                                                                                                                                                   | 7                                                        |

### 4.4 The page frame and navigation

| Component                                                                                                                                                                                                                                                                  | Purpose                                                                                                                                                            | Key props / variants                                                                                                                                                                                                   | Accessibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Most used                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Sidebar<br>+ Provider, Content, Header, Footer, Separator, Input, Rail, Trigger, Group, GroupLabel, GroupAction, GroupContent, Menu, MenuItem, MenuButton, MenuAction, MenuBadge, MenuSkeleton, MenuSub, MenuSubItem, MenuSubButton, Inset, useSidebar, useOptionalSidebar | **The library's largest component — 1,043 lines, 24 exports.** The application's left panel: a fixed column on the desktop, a `Sheet` on mobile                    | `side`: left/right · `variant`: sidebar/floating/inset · `collapsible`: offcanvas/icon/none. Plus four string props for localisation: `mobileTitle`, `mobileDescription`, **`label`**, and `label` on `Trigger`/`Rail` | A panel collapsed in `offcanvas` mode stays in the DOM (it is simply shifted off the edge) and is marked **`inert`** — its controls leave the tab order and the accessibility tree. `label` makes the panel a `complementary` landmark: without it, everything not inside the consumer's own `<nav>` ends up outside any landmark. `complementary` specifically, not `navigation` — a second navigation landmark around a real `<nav>` would assert that the whole panel consists of links                                                                                                                                                                                       | shell/admin is the only full consumer; `SidebarMenu*` is also taken by workspaces and users |
| SidebarProvider                                                                                                                                                                                                                                                            | The panel's state: open/collapsed, the mobile sheet, the hotkey                                                                                                    | The state is stored in a `sidebar_state` cookie (7 days, `SameSite=Lax`); reads and writes are wrapped in a `try/catch` — in a sandboxed iframe without `allow-same-origin`, touching `document.cookie` throws         | **The `Ctrl/⌘+B` hotkey defers to a text field.** The `ownsBoldShortcut` function recognises text inputs, `textarea`, `select` and anything inside a `contenteditable` — the combination used to be intercepted on every keypress that reached the window, so inside an article's body `Ctrl+B` collapsed the panel _and_ did not bold the text                                                                                                                                                                                                                                                                                                                                  | shell/admin                                                                                 |
| SidebarInset                                                                                                                                                                                                                                                               | The main area to the right of the panel, **split into a fixed bar and a scroll region**                                                                            | **`scrollLabel`** — the name for the scroll region's tab stop                                                                                                                                                          | The root is a real `<main>`. The scroll region carries `tabIndex={0}`: a scrollable region has to be keyboard-reachable (2.1.1), and that matters most when there is nothing on the page to focus yet — while the skeletons load. The focus ring is `ring-inset`, because the parent clips overflow and an outer ring would be drawn outside its own box. The role is `group`, not `region`: a name must not add a second landmark next to `<main>`. The role and the name are set **only together**                                                                                                                                                                             | shell/admin                                                                                 |
| TopBar · TopBarIcon · TopBarActions                                                                                                                                                                                                                                        | A thin bar of page context: a coloured icon tile, breadcrumbs, actions on the right                                                                                | Ordinary `<div>` props. `TopBarActions` goes **last** among the children — `ml-auto` is what separates it from the breadcrumbs                                                                                         | Inside `SidebarInset` the bar **portals itself** into the fixed strip, which is why the scroll region starts beneath it rather than running full height beside it. The portal preserves the page's React context — which is what lets the actions area work from inside. While the bar's host is not mounted (`null`) the bar is held back, so it does not flash in the page flow and then move. `TopBarIcon` is `aria-hidden`; the context is carried by the breadcrumbs. When the panel is hidden, a `SidebarTrigger` is embedded in the bar                                                                                                                                   | 9 (TopBarActions — 3)                                                                       |
| Breadcrumb<br>+ List, Item, Link, Page, Separator, Ellipsis                                                                                                                                                                                                                | Breadcrumbs                                                                                                                                                        | `BreadcrumbLink asChild` — for a router link                                                                                                                                                                           | A real `<nav aria-label="breadcrumb">` and `<ol>`; the current page is `aria-current="page"`; the separator and the ellipsis are `role="presentation"` + `aria-hidden`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 10                                                                                          |
| TabNav · TabNavLink                                                                                                                                                                                                                                                        | Tabs **bound to routes**: visually identical to `TabsList`, but this is a `<nav>` of links                                                                         | `TabNavLink asChild` — a router `NavLink` is passed in                                                                                                                                                                 | Activeness rides on the `aria-current="page"` that `NavLink` sets itself, and the underline hangs on exactly that. An `aria-label` is required from the caller                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2                                                                                           |
| Tabs<br>+ List, Trigger, Content                                                                                                                                                                                                                                           | Tabs whose active panel is component state rather than a route                                                                                                     | Radix's props                                                                                                                                                                                                          | Radix: `tablist`/`tab`/`tabpanel`, arrow keys. `TabsTrigger` must sit inside a `TabsList`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 1                                                                                           |
| Pagination<br>+ Content, Item, Link, Previous, Next, Ellipsis                                                                                                                                                                                                              | Page navigation                                                                                                                                                    | `PaginationPrevious`/`Next` take a `label` (the visible caption) and an `aria-label`; the defaults are the English “Previous”/“Next” and “Go to previous/next page”                                                    | A `<nav role="navigation">` and a `<ul>`; the ellipsis is `aria-hidden`. The caller marks the active page with `aria-current="page"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 5                                                                                           |
| Container · ContainerHeader                                                                                                                                                                                                                                                | The page body's wrapper (centring, a 6xl maximum, padding) and the page header                                                                                     | `ContainerHeader`: `title`, `subtitle`, `actions`, `titleClassName`                                                                                                                                                    | **`title` renders as an `<h1>`** — the canonical way to give a page its single heading. The actions cluster is `flex-wrap`: a header with three buttons on a narrow screen used to run off the right edge instead of wrapping to a second line                                                                                                                                                                                                                                                                                                                                                                                                                                   | 34 / 14                                                                                     |
| Card<br>+ Header, Footer, Title, Description, Content                                                                                                                                                                                                                      | A card surface                                                                                                                                                     | `CardTitle asChild` — so that on the sign-in screens the card's title is a real `<h1>`                                                                                                                                 | The title is a `<div>` by default for the same reason as in `Alert`/`Empty`: the rank is the page's decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 36                                                                                          |
| Table<br>+ Header, Body, Footer, Head, Row, Cell, Caption                                                                                                                                                                                                                  | A data table                                                                                                                                                       | `TableHead scope` — `col` by default, overridable for a row header                                                                                                                                                     | **The wrapper becomes a keyboard scroll region only when the content actually overflows it.** The `useOverflows` hook watches three sources — a `ResizeObserver` on the wrapper, a `ResizeObserver` on the table itself (it grows when a column is shown, while the `w-full` wrapper does not move) and the window's `resize`. While there is no overflow, no stray tab stop appears in any of the admin UI's nine tables. The stop comes with a `ring-inset` and the role `group` — `region` would add a second landmark next to `<main>`. `scope="col"` by default: a browser guesses the association from position, and that is right up to the first `colspan` or row header | 11                                                                                          |
| StatTile                                                                                                                                                                                                                                                                   | A compact metric tile: a large value above a muted caption                                                                                                         | `label`, `value`, `icon` (a component, not a string), `className`                                                                                                                                                      | The value is set in `tabular-nums` — a column of digits does not jitter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 1                                                                                           |
| Avatar<br>+ Image, Fallback, AVATAR_COLORS, avatarColorVar                                                                                                                                                                                                                 | An avatar with an initials fallback                                                                                                                                | `AVATAR_COLORS` — seven accent keys; `avatarColorVar(color)` returns `var(--color-avatar-…)` for an inline style                                                                                                       | `AvatarFallback` is mandatory — in case the image did not load. White initials on each of the seven shades give ≥ 5.7:1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 6 (+5 avatarColorVar, +4 AVATAR_COLORS)                                                     |
| Logo                                                                                                                                                                                                                                                                       | The Ortha CMS mark: a hexagon in a tile plus the wordmark                                                                                                          | `size`: `sm` (6×6) / `lg` (14×14); `showLabel` (`true` by default)                                                                                                                                                     | The icon is `aria-hidden`. With `showLabel={false}` the wordmark does not disappear but moves into `sr-only` — the mark stays named                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 3                                                                                           |
| AppLoader                                                                                                                                                                                                                                                                  | The branded full-screen loader: a large mark, a spinner, a status line. For a “root” load — application start and the auth probe, when there is no page chrome yet | `label` (`'Loading…'` by default), `heading` (defaults to `label`)                                                                                                                                                     | The root is a **`<main>`**, because while the screen is shown it _is_ the whole document, and without it every node on the page was outside any landmark. `role="status"` sits on an inner block rather than the root: on the root it would override the `main` role and bring the problem back. The hidden `<h1>` is always there — this is the state a slow connection sits in longest, and the one people most often try to navigate by headings                                                                                                                                                                                                                              | 1 (identity)                                                                                |
| WizardPageSkeleton                                                                                                                                                                                                                                                         | A full-page placeholder for the creation wizard's frame — the back link, the header, the sticky step column, the step card                                         | `label`, `steps` (3), `fields` (4)                                                                                                                                                                                     | The whole block is one `role="status"`, its content `aria-hidden`. It keeps the layout still while a lazy route's chunk loads                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 2                                                                                           |
| Stepper                                                                                                                                                                                                                                                                    | The wizard's vertical progress rail: a numbered marker, a connector, a caption, a hint, an “optional” badge, a chip summarising what was entered                   | `current`, `maxReached`, `steps: StepperStep[]`, `onStepClick`, `optionalLabel`, **`stepAriaLabel(step, n)`**                                                                                                          | The marker is a real `<button>`; completed steps are clickable, future ones `disabled`. The active one is marked `aria-current="step"`. The marker's name has to come from the caller — an icon alone says nothing. A list item's key is its **position, not its caption**: the caption is the consumer's copy, and two steps may legitimately share it                                                                                                                                                                                                                                                                                                                          | 2                                                                                           |
| WizardStepCard · WizardFooter                                                                                                                                                                                                                                              | A step card with an entrance animation and a contextual footer (back / hint on the left, skip + the primary action on the right)                                   | `WizardFooter`: `hint`, `onBack`, `backLabel`, `onSkip`, `skipLabel`, `primary`                                                                                                                                        | The card is given a `key={step}` so it remounts and replays the animation. The animation is transform-only — the content is never hidden; under `prefers-reduced-motion` it is switched off. There is no copy and no i18n inside                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 3 / 2                                                                                       |

### 4.5 Utilities, contexts and hooks

| Export                                                                   | Purpose                                                                                           | Contract                                                                                                                                                                                                                                                                                                                                                                                             | Most used                                                        |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| cn                                                                       | **The utility every component's `className` prop rides on.** Seven lines: `twMerge(clsx(inputs))` | `clsx` flattens nested arrays and objects and drops falsy values; `tailwind-merge` resolves a conflict in favour of the **last** class. The practical consequence: `cn('px-4','px-8')` = `'px-8'`, so a caller's `className` always beats the component's base classes — which is what keeps a targeted exception possible. With no arguments it returns an empty string                             | 85                                                               |
| AppearanceProvider<br>useAppearance<br>DEFAULT_THEME · THEME_STORAGE_KEY | The application's theme                                                                           | `{ theme, resolvedTheme, setTheme }`. `theme` is the choice (`light`/`dark`/`system`), `resolvedTheme` what is applied. The storage key is `'ortha.theme'` and the default value is `'system'`. Outside a provider the hook returns the defaults and a no-op setter                                                                                                                                  | the provider — bootstrap; the hook — users (the settings screen) |
| DesignSystemLabelsProvider<br>useDesignSystemLabels                      | The one localisation seam for the library's own “furniture”                                       | Today the `DesignSystemLabels` type has **one key**: `close`. The provider takes a `Partial` and merges it over the English defaults — adding a key does not break a consumer that never heard of it. A prop at the call site (`closeLabel`) still wins                                                                                                                                              | bootstrap/admin                                                  |
| useIsMobile                                                              | The single layout breakpoint the chrome looks at: **768px** (768 itself is already desktop)       | It is read **during render** by the `Sidebar` component, which mounts either the desktop panel or a `Sheet` on the strength of the answer — so the value has to be right on the **first** frame. The hook used to start at `undefined` and fill in from an effect, so a phone got one frame of the desktop layout. Outside a browser (SSR, a bare jsdom) it answers “not mobile” rather than failing | 2 (shell)                                                        |
| useSidebar · useOptionalSidebar                                          | The sidebar's state                                                                               | `useSidebar` outside a `SidebarProvider` **throws**; `useOptionalSidebar` returns `null` — for components that adapt to the panel without requiring it (which is exactly how the built-in trigger in `TopBar` works on public pages)                                                                                                                                                                 | shell/admin                                                      |
| buttonVariants · badgeVariants                                           | The CVA class generators, exported separately from the components                                 | Needed where an element must not be a `<button>` — a router link, say — while still looking like one. Used by `content/admin` (×3) and internally by `PaginationLink`                                                                                                                                                                                                                                | 3                                                                |

## 05. The hand-written primitives

Not everything in `src/lib/components/ui/` came from the CLI. Some files were written from scratch in this repository — either because the shadcn registry has no such component at all, or because what is needed is a product primitive rather than a generic one. Telling them apart matters: an upstream update **must not touch them**, and for any edit inside them there is nobody to say “here is how shadcn does it”.

| File                                                           | Why it was written by hand                                                                                                                                                              | What would happen without it                                                                                         |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| top-bar.tsx                                                    | A product page-context bar in the spirit of incident.io. The registry has no equivalent                                                                                                 | Every plugin would draw its own header; and nobody would have thought to portal it out of the scroll region          |
| app-loader.tsx                                                 | The branded root loader, from `Logo` + `Spinner`                                                                                                                                        | A loading screen with no `<main>` and no `<h1>` — the state a user on a slow connection spends the most time in      |
| logo.tsx                                                       | The product's mark. By definition it cannot come from somebody else's registry                                                                                                          | —                                                                                                                    |
| input-field.tsx                                                | The composite form field. shadcn gives the bricks (`Field`, `Input`) but not the finished assembly                                                                                      | Every form would wire up `htmlFor`, `aria-invalid` and `aria-describedby` by hand — and some would forget            |
| confirm-dialog.tsx                                             | The product scenario “confirm a dangerous action”, assembled on top of `Dialog`                                                                                                         | Twenty call sites with their own confirmation markup and their own (or absent) double-submit guard                   |
| multi-select.tsx                                               | **shadcn has no multiple select.** Assembled from `Popover`+`Command`+`Badge`                                                                                                           | Either somebody else's dependency, or a home-made list with no `aria-checked`                                        |
| date-picker.tsx                                                | A wrapper over `Calendar`: a date field and a “date+time” field                                                                                                                         | Every plugin would reassemble a calendar popover — and lose the seconds on the `HH:mm` round trip                    |
| segmented-control.tsx                                          | The product's look for a toggle, over Radix `ToggleGroup`                                                                                                                               | A loop over `Button`s with a hand-rolled active state — exactly what the shadcn skill's rule forbids                 |
| search-toolbar.tsx                                             | The standard toolbar of a list page                                                                                                                                                     | Search and filters would sit in different places on different pages                                                  |
| stat-tile.tsx                                                  | A metric tile over `Card`                                                                                                                                                               | —                                                                                                                    |
| container.tsx                                                  | The page body's wrapper and the header with its `<h1>`                                                                                                                                  | Everyone would have their own page heading, and the “one `<h1>` per page” rule would rest on discipline              |
| tab-nav.tsx                                                    | Tabs made of links. shadcn only has state-driven `Tabs`                                                                                                                                 | Router tabs would be drawn as `Tabs` — that is, as a `tablist`, which they are not                                   |
| wizard.tsx                                                     | The shared chrome of a multi-step wizard: `Stepper`, `WizardStepCard`, `WizardFooter`                                                                                                   | The workspace-creation wizard and the member-invite wizard would drift apart visually                                |
| wizard-page-skeleton.tsx                                       | A placeholder matching the wizard's layout exactly (`max-w-[920px]`, `lg:grid-cols-[244px_1fr]`)                                                                                        | A layout jump when the chunk loads                                                                                   |
| sonner.tsx                                                     | Formally a CLI component, but rewritten almost entirely: the theme from `useAppearance`, soft semantic surfaces for typed toasts, and the “click a toast, that toast dismisses” handler | Clicking a toast would dismiss **the whole stack**, unread ones included                                             |
| labels/index.tsx · appearance/index.tsx · hooks/use-mobile.tsx | The furniture-localisation context, the theme context, the single breakpoint. Infrastructure, not components                                                                            | An English close cross in a German session; a flash of the wrong theme on load; a frame of desktop layout on a phone |
| utils.ts                                                       | `cn`. It is a CLI file, but it is the foundation of everything else                                                                                                                     | —                                                                                                                    |

### CLI components rewritten for the product

A separate category: the file came from shadcn, but its behaviour is already ours. These are the most dangerous places during an upstream update — the edits are easy to lose there.

- **`alert.tsx`** — the title stopped being an `<h5>`, and an `AlertTitleContext` was added so that `aria-labelledby` is written only when the title is actually mounted.
- **`spinner.tsx`** — `role="status"` was removed and the component declared decorative.
- **`skeleton.tsx`** — `SkeletonRegion`, which does not exist upstream at all, was added, along with the pulse suppression under `prefers-reduced-motion`.
- **`table.tsx`** — the `useOverflows` hook, the conditional tab stop, the `group` role and the default `scope="col"` were added.
- **`sidebar.tsx`** — `useOptionalSidebar`, `InsetTopBarContext`, splitting `SidebarInset` into a bar and a scrollport, `inert` on a collapsed panel, `Ctrl/⌘+B` deferring to text fields, four localisation props.
- **`dialog.tsx` / `sheet.tsx`** — the `closeLabel` prop, defaulting from the labels context.
- **`popover.tsx`** — the `container` prop, for portalling inside a scroll-blocking ancestor.
- **`badge.tsx` / `button.tsx`** — extended variant sets (five “soft” ones on the chip, `brand-text` on the link button), and `gap-1` in the chip's base.
- **`empty.tsx` / `card.tsx`** — `asChild` on the title, so the page decides the rank.
- **`pagination.tsx`** — a `label` prop on the back/forward buttons alongside `aria-label`.

## 06. Rules and conventions for authors

> **Two rules that apply to every component**
>
> **1. Relative imports, not `@/`.** The consuming application's Vite dev server does not resolve the library's `@/*` alias when the admin UI eats the design system's sources — it fails with `Failed to resolve import "@/lib/utils"`. From a file in `src/lib/components/ui/` that means `import { cn } from '../../utils'`, a neighbouring component `'./<x>'`, a hook `'../../hooks/<x>'`. The shadcn CLI generates exactly `@/` imports, so they have to be rewritten **every time**. Verified against the code: all 48 files use relative paths, and there is not one `@/` import in `src/`.
> **2. Export from `src/index.ts`.** That is the package's one public surface. A component that did not reach the barrel does not exist for consumers — `package.json` hands out no subpaths other than `./styles.css` and `./package.json`. The rule covers types too (`ButtonProps`, `MultiSelectOption`, `StepperStep` and so on).
>
> Both rules apply identically to a component added by the CLI and to one written by hand.

### File layout

- **UI components** → `src/lib/components/ui/`, with the file name in kebab-case (`input-field.tsx`) and the exported symbols in PascalCase.
- **Utilities** → `src/lib/`; **hooks** → `src/lib/hooks/`.
- **Theme tokens** — in `apps/admin/src/styles.css`, not in the package.
- **Global rules** that cannot be expressed as a Tailwind utility — in `src/styles.css`; the host imports it once, after `@import 'tailwindcss'`.
- **Tests** — next to the component, `<component>.spec.tsx`.

### TypeScript conventions

- `type` rather than `interface`. (There are exceptions in the code — see section 13.)
- A JSDoc on every exported symbol. That _is_ the library's documentation: there is no showcase.
- `import type` for type-only imports.
- No `.js` extensions in imports.
- Prettier: 4-space indent, single quotes (the whole repository's rule).

### The style rules that govern working with components

They are held by the `shadcn` skill — and they apply both inside the library and in any plugin that consumes it:

#### Styling

`className` is for layout, not for recolouring. No `space-x-*`/`space-y-*` — only `flex` + `gap-*`. `size-*` when width equals height. `truncate` instead of three utilities. **No hand-written `dark:` colour overrides** — the semantic tokens are there for that. Conditional classes through `cn()`, not through template strings. No hand-set `z-index` on overlays — they manage their own stacking.

#### Forms

A form is a `FieldGroup` + `Field`, not a `div` with vertical spacing. Inside an `InputGroup`, only `InputGroupInput`/`InputGroupTextarea`. A set of 2–7 options is a `ToggleGroup`, not a loop over buttons with a hand-rolled active state. Related checkboxes/radios are a `FieldSet` + `FieldLegend`. Validity: `data-invalid` on the `Field`, `aria-invalid` on the control.

#### Composition

An item always sits inside its group (`SelectItem` → `SelectGroup`, `CommandItem` → `CommandGroup`). A custom trigger goes through `asChild`. `Dialog`, `Sheet` and `Drawer` **always** require a `Title` (it may be `sr-only`). A card is used with its full set of parts. `Button` has no `isLoading` — the state is assembled from `Spinner` + `disabled`. `Avatar` always comes with an `AvatarFallback`.

#### Component first, markup second

A callout is an `Alert`, not a styled `div`. An empty state is `Empty`. A notification is `toast()`. A divider is `Separator`, not an `<hr>`. A placeholder is `Skeleton`, not your own `animate-pulse`. A label is a `Badge`, not a coloured `span`.

The shadcn CLI is always run naming the workspace: `npx shadcn@latest add <component> -c packages/design-system`. From the repository root it fails with `monorepo_root`, because `components.json` lives in the package rather than at the root.

## 07. Accessibility: what the library does on the consumer's behalf

The target is WCAG 2.1 level AA, as engineering practice (there is no formal Section 508 / VPAT commitment today). Automated checks catch a fraction of the violations: they guard against regressions but do not prove conformance. Below is what is already built into the primitives, and what a primitive cannot provide.

### What you get for free

| What                                                                                        | Where it is implemented                                                                                                                                                                                                                                              | Criterion           |
| ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| The label is tied to the field, the error is tied to the field, the field is marked invalid | `InputField` wires up `htmlFor`/`id`, `aria-invalid` and `aria-describedby` (hint + error + any user-supplied value)                                                                                                                                                 | 1.3.1, 3.3.1, 3.3.2 |
| A new error is announced                                                                    | `FieldError` — `role="alert"`, and **only when there is something to say**                                                                                                                                                                                           | 4.1.3               |
| A loading state is announced exactly once                                                   | `SkeletonRegion` — one named `role="status"` + `aria-busy`; `Skeleton` and `Spinner` stay silent                                                                                                                                                                     | 4.1.3               |
| The landmarks are in place                                                                  | `SidebarInset` — a real `<main>`; `AppLoader` — a `<main>`; `Sidebar label` — `complementary`; `Breadcrumb`/`TabNav`/`Pagination` — `<nav>`                                                                                                                          | 1.3.1, 2.4.1        |
| A scrollable region is keyboard-reachable and **visible** once reached                      | `SidebarInset` (the scrollport) and `Table` (the wrapper) — `tabIndex={0}` + `ring-inset`; on the table the stop is taken only on real overflow                                                                                                                      | 2.1.1, 2.4.7        |
| Focus is visible in forced-colors mode too                                                  | `@media (forced-colors: active)` in `src/styles.css` — a real `outline` instead of a `ring`                                                                                                                                                                          | 2.4.7, 1.4.11       |
| A hidden panel does not catch the Tab key                                                   | `inert` on a collapsed offcanvas panel                                                                                                                                                                                                                               | 2.4.3               |
| Headings do not skip ranks                                                                  | `AlertTitle`, `CardTitle`, `EmptyTitle` are `<div>` by default; the page sets the rank — through `asChild` on `CardTitle` and `EmptyTitle`, and by nesting a heading inside `AlertTitle`, which takes none because it stays the element the banner names itself with | 1.3.1               |
| A table is really marked up as one                                                          | `<table>`, `<th scope="col">` by default, `<caption>`                                                                                                                                                                                                                | 1.3.1               |
| An icon control is named                                                                    | `SidebarTrigger`, `SidebarRail` and the `Dialog`/`Sheet` close cross — an `sr-only` name whose default a localised string overrides                                                                                                                                  | 4.1.2, 3.1.2        |
| Meaning is not carried by colour alone                                                      | `SegmentedControlItem` and `SidebarMenuButton` — a fill **plus** a heavier weight; `MultiSelect` — `aria-checked`, not just a tick; status dots always with text                                                                                                     | 1.4.1               |
| Motion respects the system setting                                                          | `Skeleton` (`motion-reduce:`) and the three `prefers-reduced-motion` blocks in `src/styles.css` — the dropdown and the wizard step are switched off, the spinner’s rotation is replaced by an opacity breath                                                         | 2.3.3               |
| An overlay traps and returns focus and closes on Escape                                     | Radix — `Dialog`, `Sheet`, `Popover`, `DropdownMenu`, `Select`, `Tooltip`                                                                                                                                                                                            | 2.1.2, 2.4.3        |
| A notification is announced                                                                 | sonner's live region under `Toaster`                                                                                                                                                                                                                                 | 4.1.3               |
| A disabled control reads as disabled in high contrast too                                   | `color: GrayText` in `forced-colors`: `opacity` is not part of the forced palette                                                                                                                                                                                    | 1.4.1               |

### What the library cannot provide

- **A control's name that depends on the data.** A kebab in a table row must be called “Actions for {name}” — the component does not know which row. Likewise `Stepper` requires `stepAriaLabel`, `SidebarInset` a `scrollLabel`, `Table` an `aria-label`/`<caption>`, and `Progress` an `aria-label` and, where needed, an `aria-valuetext`.
- **A heading's rank.** The page decides, not the banner and not the card.
- **Moving focus after a destructive action.** Hence `ConfirmDialog`'s `onCloseAutoFocus` and `SearchToolbar`'s `inputRef`: the library provides the hook, but the caller must do the aiming.
- **The “skip to main content” link.** It is drawn by `AppShell` in `shell/admin`, and `SidebarInset` only supplies the target — `<main id="main-content" tabIndex={-1}>`. Adding it again in a plugin is not allowed.
- **`<html lang>` and `<html dir>`.** They are set by `createAdmin` from its locale. A component must not do it.
- **Translating strings.** Every visible text comes from outside; the library only supplies English defaults for pure iconography.

> **Two role="alert"s in one view is a conflict**
>
> `FieldError` and `Alert` both declare themselves `alert`. A form showing a general error banner and a field error at once creates two live regions competing for the right to speak. That is exactly the ailment `Spinner` was cured of. The rule is not to put two such regions on one screen.

> **English names that cannot be overridden**
>
> Not all the furniture has been routed through the labels context. `Breadcrumb` hard-codes `aria-label="breadcrumb"` and `Pagination` `aria-label="pagination"`; neither has a prop to override it. That is not a 4.1.2 failure (there is a name), but it is a 3.1.2 of the same class as the one already fixed for the dialog's close cross: in a German session a landmark is named in English. The pagination's back/forward buttons, unlike the `<nav>`, can be overridden — they have both a `label` and an `aria-label`.

### Where this is checked

Not in this package. Accessibility is measured in `apps/admin-e2e`, where the interface really exists: 7 `a11y.spec.ts` suites (axe) and 4 `keyboard.spec.ts` suites. Plus three host-level suites:

- `host/host.spec.ts` — “bypass blocks”: the first `Tab` lands on the skip link, it becomes visible on focus, and activating it moves focus to `<main>`.
- `host/reflow.spec.ts` — the same at 320px: a skip link a narrow layout pushed off screen is not a skip link.
- `host/platform-preferences.spec.ts` — **the one place the dark palette is scanned** (every other axe run is in the light theme) and where the focus indicator is checked to survive `forced-colors: active`. An important harness detail: `test.use({ forcedColors })` and `test.use({ reducedMotion })` in Playwright **do not work** — those properties are not in the fixed list of context options; `page.emulateMedia()` and a project-level setting are what do.

## 08. Working scenarios — step by step

### 8.1 Adding a component from the shadcn registry

1. **Check that it is not already there.** In `src/index.ts` first, then `npx shadcn@latest search`. “Use what exists first” is the skill's first rule for a reason: `MultiSelect`, `SearchToolbar` and `ConfirmDialog` already exist and are not in the registry.
   _the task is often solved by composition rather than by a new file_
2. **Run the CLI naming the workspace.** `npx shadcn@latest add <component> -c packages/design-system`. Without `-c` the command fails with `monorepo_root`.
3. **Rewrite the `@/` imports as relative ones.** Mandatory step 1. Otherwise the admin dev server will not come up.
4. **Add the exports to `src/index.ts`.** Mandatory step 2. Including the `*Props` types.
5. **Check the colours.** A registry component arrives on the `neutral` base palette; if it uses a token that is not in `apps/admin/src/styles.css`, either add it there (checking contrast) or swap it for an existing semantic one.
6. **Write the JSDoc** on every exported symbol — there is no showcase, and this is the only documentation.
7. **Check the build and the types.** `npx nx typecheck design-system`, then `npx nx lint design-system`. Tailwind picks up a new directory itself — the `@source` glob in `apps/admin/src/styles.css` already covers `src/**/*.{ts,tsx}`.
8. **Decide whether a test is needed.** By the criterion in section 9: a test here is justified only if this is a seam a page test cannot reach.

### 8.2 Changing an existing component

1. **Work out who will be affected.** `grep -rl "<ComponentName" packages apps`. For `Button` that is 146 files — changing a default variant is visible across the whole admin UI.
2. **Check whether the task can be solved at the call site.** `cn` is built so a caller's `className` beats the base classes. If the exception is a one-off, it should stay a one-off.
3. **If the change is shared, make it in the shared layer.** The cursor on buttons, the focus ring in high contrast, the pulse suppression — all of them are “one file instead of thirty” edits. The sign is that the property belongs to “is this operable at all?” rather than to a particular component.
4. **Do not change a role without reason — and change it with an explanation.** Every case in this library where a role or an attribute differs from upstream (`Alert`, `Spinner`, `Table`, `SidebarInset`) carries a comment with a ticket number. That is a convention: whoever comes next to “fix it back” must first read why.
5. **Do not hard-code a string you are adding.** Either a prop at the call site, or a key in `DesignSystemLabels` (the type is a `Partial`, so a new key does not break a consumer).
6. **Check both themes.** The tokens are declared twice; an edit tied to a particular lightness usually breaks in the dark theme.
7. **Run the package's tests and the e2e.** `npx nx test design-system`, then `npx nx e2e admin-e2e -- --project=chromium src/**/a11y.spec.ts src/**/keyboard.spec.ts`.

### 8.3 Updating a component from upstream

The riskiest of the three scenarios: shadcn is a copy of the sources, so an “update” physically overwrites a file that may contain our edits.

1. **Commit the current state to git.** The update should be its own commit, so the diff shows exactly what arrived.
2. **Check whether it is on the rewritten list.** Section 5 lists the files where upstream has already diverged from us. For those, an update is a **manual merge** rather than an overwrite.
3. **Run the CLI.** `npx shadcn@latest add <component> -c packages/design-system` — the command will overwrite the file (the CLI asks for confirmation).
4. **Read the whole diff.** Look for: vanished `aria-*`, a returned `<h5>` on the title, a returned `role="status"` on the spinner, a vanished `scope`, a vanished prop (`closeLabel`, `container`, `label`).
   _every such loss is an accessibility regression, not cosmetics_
5. **Rewrite the `@/` imports again.** The CLI generates them every time.
6. **Reconcile the exports with `src/index.ts`.** Upstream may have renamed or added a subcomponent.
7. **Check for dead animation classes.** If `animate-in`/`fade-in-0`/`zoom-in-95` arrived, they will generate no CSS: the corresponding Tailwind plugin is not installed in the repository. Either remove them or express the motion through `.ds-dropdown-motion`.
8. **Run the package's tests.** The `localizable-copy`, `table`, `sidebar` and `input-field` suites were written precisely so that such a regression fails here rather than in production.

### 8.4 Adding or changing a theme token

1. **Open `apps/admin/src/styles.css`** — not a file in the package.
2. **Declare it in `@theme`** (light) **and in `.dark`** (dark). A missing half is not “inheritance” but a token that stays light in the dark theme.
3. **Check the contrast**: 4.5:1 for ordinary text, 3:1 for large text, 3:1 for borders and graphics. The axe run in `admin-e2e` checks `color-contrast`, and a regression here fails CI.
4. **Remember the palette's two traps.** White text on `bg-brand` does not pass AA — orange is meant for icons and accents. In the dark theme the solid status fills invert _their own foreground_ to ink: a lightened fill no longer carries white text to AA.
5. **If the token is substituted inline** (as the avatar palette is), declare it in `:root` rather than in `@theme`: Tailwind prunes from `@theme` any token no generated utility referenced.
6. **For the chart palette** — do not touch the values by hand: they are in hex because they are a colour validator's output. Changing them requires re-running the validator against the card surface.

## 09. The package's tests

A vitest + Testing Library suite in a `jsdom` environment: `npx nx test design-system`. Twenty-three files, 189 cases, all next to their component.

> **This is not a coverage exercise**
>
> What lands here is **a seam a page test cannot reach**: a prop combination no live consumer passes (`invalid={false}` next to an error), a collision a real dataset does not produce (two options with the same caption), a value invisible to a browser test (the seconds in a date-time), or a system setting the e2e harness does not emulate. Everything a real page exercises lives in `apps/admin-e2e`, where it is exercised for real.

| File                      | Cases | What exactly is pinned                                                                                                                                                                                    |
| ------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| sidebar.spec.tsx          | 17    | The provider, the cookie state, `Ctrl/⌘+B` deferring to text fields, `inert` on a collapsed panel, `SidebarInset`'s scrollport and its name                                                               |
| input-field.spec.tsx      | 15    | `InputField` and `FieldError`: the `aria-describedby` assembly, `invalid={false}` not hiding the message, and `errors={[]}` and `errors={[{}]}` not opening an empty `alert`                              |
| multi-select.spec.tsx     | 12    | Two options with the same caption stay distinguishable; searching by `value`; `aria-checked`; `aria-haspopup="listbox"`                                                                                   |
| table.spec.tsx            | 10    | The tab stop appears only on overflow; the `group` role and the name are set together; `scope="col"`                                                                                                      |
| appearance/index.spec.tsx | 10    | Hydration from `localStorage`, reacting to a system theme change, the `dark` class and `color-scheme`, resilience to unavailable storage, the hook's behaviour outside a provider                         |
| confirm-dialog.spec.tsx   | 7     | `busy` locks the dialog and disables both buttons; `onCloseAutoFocus` is passed through                                                                                                                   |
| date-picker.spec.tsx      | 7     | Seconds survive the round trip; changing the day preserves the time, changing the time preserves the day                                                                                                  |
| wizard.spec.tsx           | 7     | `Stepper`: completed steps are clickable, future ones disabled, `aria-current="step"`, two steps with the same caption do not merge                                                                       |
| localizable-copy.spec.tsx | 7     | **The key suite.** That a localised string can reach the `Dialog`/`Sheet` close cross, `SidebarTrigger`/`SidebarRail` and the `Pagination` buttons at all; and that without one the default stays English |
| use-mobile.spec.tsx       | 6     | The right value on the first frame; subscribing to and unsubscribing from `matchMedia`                                                                                                                    |
| tooltip.spec.tsx          | 4     | `Tooltip` and the tooltip on `SidebarMenuButton`                                                                                                                                                          |
| progress.spec.tsx         | 4     | `role="progressbar"`, `aria-valuenow`                                                                                                                                                                     |
| top-bar.spec.tsx          | 4     | Portalling into the inset's bar; rendering in place when there is no inset; holding back while the host is not mounted                                                                                    |
| utils.spec.ts             | 4     | `cn`: the last class wins a conflict, falsy values are dropped, arrays are flattened, an empty call gives `''`                                                                                            |
| sonner.spec.tsx           | 3     | Clicking a toast's body dismisses **that** toast; a click on an interactive child is not intercepted                                                                                                      |

<details>
<summary>Why nothing runs without `src/test-setup.ts`</summary>

jsdom implements neither `matchMedia` nor `ResizeObserver` nor the Pointer Events that Radix's presence/dismissable layers subscribe to, and it stubs out `scrollIntoView`. Without the shims, mounting **any** component built on `Popover`, `Command` or `Sheet` throws before an assertion is reached — that is, the seams these tests exist for would be not “untested” but “untestable”. The shims are installed conditionally (`if (!window.matchMedia)`) so a real implementation, where one exists, is not replaced.

</details>

<details>
<summary>Two platform settings that are checked elsewhere</summary>

`apps/admin-e2e/src/host/platform-preferences.spec.ts` — because they are properties of the **assembled** stylesheet rather than of a single component. That is where the dark palette is scanned (every other axe run is in the light theme) and where the focus indicator is checked to survive `forced-colors: active`, in which the browser discards `box-shadow` and with it every one of this library's Tailwind `ring-*`s.

</details>

## 10. Invariants

Statements that must always hold. This is at once a review list and a draft set of test assertions.

- **I-01** — No file in `src/` imports through the `@/` alias — only through relative paths.
- **I-02** — Everything meant for a consumer is exported from `src/index.ts`; the package hands out no public subpaths other than `./styles.css` and `./package.json`.
- **I-03** — No component contains a literal colour: only semantic Tailwind utilities expanding into `var(--color-*)`.
- **I-04** — Every light-palette token is overridden in `.dark`, except three families that are the same colour in both themes on purpose: the panel’s chrome (`--color-sidebar-*`), the navigation accents (`--color-nav-*`) and the avatar palette (`--color-avatar-*`). Nothing else may inherit, and an inherited family that acquires a `.dark` override has left the list.
- **I-05** — A caller's `className` prop always beats the component's base classes: `cn` puts it last and `tailwind-merge` gives a conflict to the last one.
- **I-06** — The package contains neither `react-intl` nor a translation catalogue; every visible string either arrives as a prop or has an English default overridable through `DesignSystemLabelsProvider`.
- **I-07** — `DesignSystemLabelsProvider` takes a `Partial`: a missing key keeps its English default, so adding a key does not break an existing consumer.
- **I-08** — A prop at the call site (`closeLabel`, `label`) always beats the value from the labels context.
- **I-09** — `Spinner` and `Skeleton` are decorative and announce nothing themselves; busyness is announced only by a named `role="status"` (`SkeletonRegion`, `AppLoader`, `WizardPageSkeleton`).
- **I-10** — There is exactly one live region per loading surface; two mounted at once is a defect.
- **I-11** — `FieldError` returns `null` when there are no messages; an empty `role="alert"` is never opened.
- **I-12** — `invalid` in `InputField` governs only the control's marking and never suppresses the error message.
- **I-13** — A control's `aria-describedby` is assembled from the hint, the error and the value the caller passed — none of the three is lost.
- **I-14** — `AlertTitle`, `CardTitle` and `EmptyTitle` are not headings by default; the page sets the rank — through `asChild` on `CardTitle` and `EmptyTitle`, and by nesting a heading inside `AlertTitle`, which takes no `asChild` because it stays the element the banner’s `aria-labelledby` points at.
- **I-15** — `Alert` writes `aria-labelledby` only after `AlertTitle` has actually mounted.
- **I-16** — The `Table` wrapper takes a tab stop **only** on actual overflow; the `group` role and the name are set exclusively together.
- **I-17** — `SidebarInset`'s scrollport is focusable and draws a visible ring when it receives focus; the ring is `inset`, because the parent clips overflow.
- **I-18** — No nameable container in the library adds a second landmark: `group` is used, not `region`. The only landmarks the library creates are the `<main>` of `SidebarInset`/`AppLoader`, the `complementary` of a named panel, and the `<nav>` of the breadcrumbs, the link tabs and the pagination.
- **I-19** — A collapsed offcanvas panel is marked `inert`: its controls are outside the tab order and outside the accessibility tree.
- **I-20** — `Ctrl/⌘+B` is not intercepted when focus is in a text input, a `textarea`, a `select` or inside a `contenteditable`.
- **I-21** — `useIsMobile` returns the right value on the first render frame, not after an effect.
- **I-22** — The library's single layout breakpoint is 768px; 768 inclusive counts as desktop.
- **I-23** — Every access to `localStorage`, `document.cookie` and `matchMedia` is wrapped in an environment check and a `try/catch`; a storage failure loses only the persistence of the preference.
- **I-24** — The theme is applied to the document in two ways at once: the `dark` class on `<html>` and `style.colorScheme` — so the browser repaints its own furniture too.
- **I-25** — `useAppearance` outside an `AppearanceProvider` does not throw: it returns the defaults and a no-op setter.
- **I-26** — `useSidebar` outside a `SidebarProvider` throws; for an optional dependency there is `useOptionalSidebar`.
- **I-27** — `TopBar` inside an inset renders only through a portal into the bar; while the bar's host is `null` the bar is not drawn at all, so it does not flash in the page flow.
- **I-28** — Clicking a toast's body dismisses that toast; clicks on the close button, the action/cancel buttons and links inside the toast are not intercepted.
- **I-29** — Toasts are positioned in the bottom right corner, and that is decided in `Toaster` rather than at the call site: `copilot-admin` relies on it when suppressing a toast while the dock is open.
- **I-30** — Every non-operable animation is suppressed under `prefers-reduced-motion: reduce` — with one deliberate exception, the loading spinner, which is **stripped of movement rather than stopped**: opacity only, no `transform`, a cycle no faster than 1 s. A stopped indeterminate loader is indistinguishable from a hung request, and WCAG 2.3.3 is about non-essential _motion_, not about withholding essential feedback.
- **I-31** — The wizard step's animation changes only the transform; opacity stays at 1, so the content is visible even with animation clocks stopped.
- **I-32** — Under `forced-colors: active`, every `:focus-visible` has a real `outline`; `forced-color-adjust: none` is used nowhere.
- **I-33** — No state is carried by colour alone: a selected segment and an active panel item additionally thicken the font, and status dots are always accompanied by text.
- **I-34** — White text is never placed on `bg-brand`; there is a separate `brand-text` token for link orange.
- **I-35** — A wizard step list item's key is its position, not its caption: the caption belongs to the consumer and may repeat.
- **I-36** — A row in `MultiSelect` is identified by its `value`, with the caption added as `keywords` — otherwise two options with the same caption merge into one.
- **I-37** — The package exports nothing server-side and has no routes, no module, no tables and no permission keys.
- **I-38** — The only peer dependencies are `react` and `react-dom` at `^19`; everything else is an ordinary dependency, so a consumer has nothing extra to install.
- **I-39** — When `InputField` shows an error, the hint leaves the **screen** and not the document: it keeps its id, so the `aria-describedby` of `I-13` still resolves and the instruction stays available to the reader who just tripped the rule it states.
- **I-40** — `FieldError` and `FieldDescription` carry the same type metrics — size, weight, line box and the `nth-last`/`last` rhythm. They occupy one slot under a control, one after the other, so colour is the only thing that may differ between them.
- **I-41** — `SegmentedControl`'s `field` variant matches `Input` on radius, border token and height. A segmented control used as a form control is not styled as the toolbar variant, and the toolbar variant stays the default so the five toolbar call sites are unaffected.
- **I-42** — `ContainerHeader`'s optional icon is `aria-hidden` and outside the `<h1>`: the heading's accessible name is its own words, and a page with a mark still reports exactly one heading.

## 11. Testing checklist

Phrased as “action → expected result”, so they can go into a test case without rewriting. The library is checked two ways: by the package's unit tests (`npx nx test design-system`) and by eye and keyboard in a live admin UI (`npm run dev`). The package has no server side, so `curl` and `psql` have nothing to do with it.

### Visual coherence

- **Walk every admin page in the light theme** → identical card radii, identical button and field heights (9), the same input border everywhere — noticeably darker than the structural one.
- **Switch to the dark theme** → every page repaints in full; not one patch stays light; cards read as a surface a step lighter than the canvas.
- **The `system` theme, with the OS theme changed while the tab is open** → the interface repaints without a reload.
- **Reload the page with the dark theme chosen** → the very first paint is dark, with no flash of light.
- **Open the admin UI in a private window with storage blocked** → the application works, the theme is `system`, and the console has no errors.
- **The sidebar in both themes** → it stays dark: its tokens are dark to begin with and are not overridden in `.dark`.
- **Check all nine `Badge` variants on a highlighted table row** → the secondary chip does not blend into the hover background.
- **An orange link on white and on dark** → readable; there is no white text on an orange fill anywhere.
- **A page header with three or more action buttons at 900px wide** → the cluster wraps to a second line rather than running off the right edge.
- **The scrollbars in every region (the inset, the panel, a table, the right-hand panel)** → thin, with the thumb in the border token's colour and a transparent track — that is, they change with the theme.

### Forms and fields

- **Click a field's label** → focus moves into the control.
- **Submit a form with an error** → the field is marked `aria-invalid`, the border is red, and the message appeared and was announced (`role="alert"`).
- **Pass `invalid={false}` together with an `error`** → the message is rendered anyway; it cannot be hidden with that prop.
- **Pass `errors={[]}` or `errors={[{}, undefined]}`** → nothing is rendered; there is no empty `role="alert"` in the DOM.
- **Focus a field with a hint and an error** → the screen reader reads out both the hint and the error — both made it into `aria-describedby`.
- **A field with the caller's own `aria-describedby`** → it is not lost but added to the generated ones.
- **A `MultiSelect` with two options sharing a caption** → the rows are distinguishable, highlight separately, and the arrow keys move between them.
- **Search a `MultiSelect` by an option's value rather than its caption** → the option is found.
- **A `MultiSelect` inside a `Dialog` without the `container` prop** → the defect reproduces: the list does not scroll with the mouse wheel. With `container` passed, it does.
- **`DateTimePicker`: type a time with seconds, then change the day** → the seconds are preserved.
- **`DateTimePicker`: pick a day, then change the time** → the day is not reset.
- **A disabled control** → a `not-allowed` cursor, a click does nothing; in high contrast the text is `GrayText`.

### Keyboard and focus

- **The first `Tab` on any private page** → the “skip to main content” link, visible while focused; activating it moves focus to `<main>`.
- **Tab through the whole page** → a focus indicator is visible at every stop; there are no “dead” presses where nothing visibly happens.
- **Tab to the inset's scroll region** → the focus ring is visible (inset), and the arrow keys scroll the content.
- **A wide table in a narrow window** → the wrapper takes a tab stop and gets a ring; in a wide window there is no extra stop.
- **Collapse the sidebar and Tab through** → not one control of the hidden panel receives focus.
- **Press `Ctrl/⌘+B` while in an input or in an article's body** → the panel does not collapse; in the editor bold is applied.
- **Press `Ctrl/⌘+B` while not in a field** → the panel toggles.
- **Open a dialog and press `Tab` many times** → focus does not leave the dialog.
- **Close a dialog with `Escape`** → focus returned to the element that opened it.
- **Confirm a deletion in a `ConfirmDialog` whose trigger button disappears** → focus lands on an existing element rather than on `<body>` (this needs an `onCloseAutoFocus` from the caller).
- **Clear the filters with the button inside the empty state** → focus returns to the search field (through `inputRef`) rather than being lost.
- **Open a dropdown menu and press an item's first letter** → focus jumps to that item (Radix type-ahead).
- **Walk a `SegmentedControl` with the arrow keys** → it switches; the whole group takes one tab stop.

### Screen-reader announcements

- **Go to a page that is loading** → announced exactly once, with the text from `SkeletonRegion`'s `label`; the individual placeholders stay silent.
- **Mount two `SkeletonRegion`s at once** → a defect: two competing live regions.
- **Perform an action that raises a toast** → the toast's text is announced.
- **The application's start screen** → it is a `<main>`, it has an `<h1>` (hidden though it is), and the status is announced once.
- **An `Alert` banner with no `AlertTitle`** → there is no `aria-labelledby` attribute in the DOM — there must be no reference to a nonexistent id.
- **An `Alert` banner with a title** → the banner's name equals the title's text; the title is not an `h*` element.
- **Walk the page by headings** → exactly one `<h1>`, the ranks in order, no skipped levels.
- **Walk the page by landmarks** → one `main`; the named sidebar is `complementary`; not one unnamed `region`.
- **The kebab menu in a table row** → the name is tied to the row rather than being “Actions”.

### Localising the furniture

- **Open the admin UI in a non-English locale and open any dialog** → the close cross is named in the session's language.
- **Mount a `Dialog` without a `DesignSystemLabelsProvider`** → the cross is named `Close` — the default is preserved.
- **Pass a `closeLabel` at the call site** → the prop wins, not the context.
- **Check the breadcrumbs' and the pagination's `<nav>` in a non-English locale** → a known divergence: their `aria-label` stays English (see section 13).
- **The pagination's back/forward buttons with a `label` and an `aria-label` passed** → both the visible caption and the name are in the session's language.

### System settings

- **Turn on `prefers-reduced-motion: reduce`** → placeholders do not pulse, a wizard step does not slide, dropdowns appear without animation; a spinner stops turning but keeps fading gently, so a pending request still looks pending; the content is visible throughout.
- **Turn on Windows high-contrast mode** → an outline is visible on every focusable element; on scrollable regions it is inset; a selected panel item and a selected option get a border; disabled controls are grey.
- **Run axe in the dark theme** → no contrast violations (checked only in `platform-preferences.spec.ts`).
- **A 320px window width** → no horizontal document scroll; the skip link stays reachable; the panel becomes a sheet.
- **Narrow the window to 767px** → the sidebar becomes a sheet immediately, with no frame of desktop layout.

### Regressions after an upstream update

- **After a `shadcn add`, run `npx nx test design-system`** → the suite stays green; a red `localizable-copy` means a lost localisation prop.
- **Look for `@/` in the diff** → not one should remain; otherwise the admin dev server will not come up.
- **Reconcile the exports with `src/index.ts`** → no previously public symbol has vanished, and the new ones are added.
- **Look for `animate-in` / `fade-in-0` / `zoom-in-95` in the diff** → if they arrived, they are dead classes: the plugin that generates them is not in the repository.
- **Look for `role="status"`, `<h5>` and a vanished `scope` in the diff** → every match is a rollback of a deliberate accessibility fix.

## 12. Boundaries of responsibility

Half the questions about the design system are really about its neighbours. The table says where to go.

| Question                                                           | Answered by                     | Why not the design system                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------ | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| “What colour is a button in the dark theme?”                       | apps/admin/src/styles.css       | The library knows only the role (`bg-primary`); the value belongs to the application                                                                                                                                                                                             |
| “Where is the ‘skip to main content’ link?”                        | shell/admin — AppShell          | The design system supplies the target (`<main id="main-content" tabIndex={-1}>` inside `SidebarInset`) but not the link itself — there is one per application                                                                                                                    |
| “Who assembles the sidebar out of items?”                          | shell/admin                     | The library provides 24 panel primitives; which items are in it and in what order is the frame's decision, and plugins fill its slots                                                                                                                                            |
| “Who mounts `Toaster`, `AppearanceProvider`, `TooltipProvider`?”   | bootstrap/admin — createAdmin   | The providers are mounted once per application; the library has no entry point of its own                                                                                                                                                                                        |
| “Who sets `<html lang>` / `dir`?”                                  | bootstrap/admin — createAdmin   | A property of the document, not of a component                                                                                                                                                                                                                                   |
| “Who translates the strings?”                                      | each plugin, through react-intl | The library has no i18n runtime; the exception is the single `close` key, filled in by the `DesignSystemLabels` wrapper in bootstrap                                                                                                                                             |
| “Who validates a form?”                                            | the plugin (TanStack Form)      | `InputField` is presentational: it takes `invalid`/`errors`, it does not compute them                                                                                                                                                                                            |
| “Who checks accessibility?”                                        | apps/admin-e2e                  | The interface really exists only in the assembled application; the package checks only the seams a page test cannot reach                                                                                                                                                        |
| “Who draws the charts?”                                            | insights/admin                  | The `--color-chart-*` tokens are declared in the host; there are no visualisation components in the library                                                                                                                                                                      |
| “Who owns the text editor's styles?”                               | wysiwyg/admin                   | It has its own `.ortha-wysiwyg` sheet, which the host imports third; it reads the same `--color-*` tokens                                                                                                                                                                        |
| “Who decides whether the package reaches a generated application?” | create-ortha-app — features.ts  | `@orthacms/design-system` is in `CORE_PACKAGES` and is installed unconditionally: it is the first thing you reach for when writing a page of your own, and an undeclared import is a phantom dependency that stops resolving on a version conflict and never resolves under pnpm |
| “Who assembles the npm tarball?”                                   | tools/release/pack.mjs          | In the repository the package deliberately resolves from source; the published manifest is a rewritten copy whose `exports` point at `./dist`, with `src/styles.css` carried across by hand                                                                                      |

> **A separate boundary: what Radix does and what we do**
>
> **Radix** owns the behaviour — roles, focus traps, roving focus, Escape, portals, focus return, type-ahead. **The library** owns the styling, the product decisions (toast position, icon-tile size, the variant set) and the accessibility work Radix cannot do because it does not know the page's context: `aria-haspopup="listbox"` on the multi-select, `aria-checked` in place of the taken `aria-selected`, the table's conditional tab stop, `inert` on a collapsed panel, the `group` role instead of `region`. The reading rule: if an attribute is set explicitly in our file, there is a reason for it, and it is written in the comment beside it.

## 13. Where the code and the documentation diverge

Every claim in the package's `AGENTS.md`, in the `shadcn` and `accessibility` skills and in the comments in the style files was checked against the implementation. Below is what diverged.

| Where                                                         | What it says                                                                                                                         | How it actually is                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| design-system/AGENTS.md, “Layout”                             | “The package also ships a **small** global stylesheet at `src/styles.css` (the `wizard-step-in` animation used by `WizardStepCard`)” | The wizard-step animation is about a quarter of the file. Also in it: the `.ds-dropdown-motion` class with two `@keyframes` and different in/out durations (the motion of `Popover`/`DropdownMenu`/`Select`), the restoration of `cursor: pointer` on every operable control in `@layer base`, and a large `@media (forced-colors: active)` block — the one place in the repository where the high-contrast focus indicator is fixed. The description gives no sense that editing this file affects focus on every page |
| .claude/skills/shadcn/SKILL.md                                | “Theme tokens (`@theme inline`) live in `apps/admin/src/styles.css`”                                                                 | What is used is `@theme`, **not** `@theme inline`, and the file itself says this is deliberate: `inline` would bake the values into the utilities and the `.dark` override would stop taking effect at runtime. The discrepancy is a dangerous one — an agent following the skill might “correct” the directive and break the dark theme entirely                                                                                                                                                                       |
| .claude/skills/shadcn/SKILL.md                                | “Stack facts (from `components.json`): style new-york, base color neutral, **`radix` primitives**, lucide icons, Tailwind v4”        | `components.json` has no field about primitives at all; it has `$schema`, `style`, `rsc`, `tsx`, `tailwind` (`css`, `baseColor`, `cssVariables`), `iconLibrary` and `aliases`. Radix is a fact about the dependencies (15 packages), not about the configuration                                                                                                                                                                                                                                                        |
| .claude/skills/accessibility/SKILL.md, “Color & contrast”     | “`muted-foreground` was darkened (from shadcn's `oklch(0.556…)` to **`0.5`**)”                                                       | The actual value in the light palette is `oklch(0.49 0.015 285)`. In the dark one it is `oklch(0.72 0.012 285)`. The number in the document matches neither                                                                                                                                                                                                                                                                                                                                                             |
| apps/admin/src/styles.css, the comment on `--color-sidebar-*` | “**Light-mode only — the admin ships no dark content theme.**”                                                                       | A dark theme exists and can be switched to: the `.dark` block is declared further down in the same file, and `AppearanceProvider` puts the class on `<html>`. What is true in substance is that the panel's tokens are **not overridden** in the dark theme, because they are written dark to begin with. The comment is a whole feature out of date                                                                                                                                                                    |
| dialog.tsx, sheet.tsx, tooltip.tsx                            | They carry the classes `animate-in`, `fade-in-0`, `zoom-in-95`, `data-[state=closed]:animate-out` and `slide-in-from-*`              | Neither `tailwindcss-animate` nor `tw-animate-css` is **installed** in the repository — those utilities generate no CSS. The comment in `src/styles.css` admits it outright (“those utilities are not installed”) and explains that this is precisely why the dropdown motion is hand-written, but the dead classes have never been removed from those three files. A developer reading them is convinced the modal is animated                                                                                         |
| design-system/AGENTS.md, “TS conventions”                     | “`type` over `interface`”                                                                                                            | Four declarations remain `interface`s: `ButtonProps` (`button.tsx`), `BadgeProps` (`badge.tsx`), `SheetContentProps` (`sheet.tsx`), `AppearanceContextValue` (`appearance/index.tsx`). The first two are public types, so the divergence is visible to a consumer                                                                                                                                                                                                                                                       |
| components.json, `aliases`                                    | Declares `"utils": "@/lib/utils"`, `"ui": "@/lib/components/ui"` and so on                                                           | Those are exactly the imports rule 1 forbids. The configuration **guarantees** the CLI will generate forbidden code, and a manual rewrite is required after every `shadcn add`. Formally this is not a documentation error — but both texts (AGENTS.md and the skill) describe the manual step as mandatory without once noting that its cause sits in the configuration next door                                                                                                                                      |
| design-system/README.md                                       | The generator's stub: “This library was generated with Nx” and one command for running tests                                         | Not a word about the two mandatory rules, about consumption from source, about theming. And `pack.mjs` puts exactly this README into the tarball — that is, the stub is what goes to npm. All the substantive documentation lives in `AGENTS.md`, which does not reach the tarball                                                                                                                                                                                                                                      |
| AGENTS.md versus README.md                                    | `npx nx test design-system` versus `nx test @orthacms/design-system`                                                                 | The project's name is `@orthacms/design-system` (taken from `package.json`). The two documents name the target differently; whether Nx accepts the short form could not be established in this environment (the dependencies are not installed)                                                                                                                                                                                                                                                                         |
| Breadcrumb, Pagination                                        | The `accessibility` skill lists them among the “Radix-backed primitives that wire roles, labelling, and focus for you”               | Both are written in plain HTML, without Radix, and both carry a **hard-coded English** `aria-label` (`"breadcrumb"`, `"pagination"`) with no prop to override it. The pagination's buttons can be overridden, the landmark itself cannot. This is the same class of 3.1.2 defect already fixed for the dialog's close cross through `DesignSystemLabelsProvider`, but the fix never reached these two landmarks                                                                                                         |
| CommandDialog                                                 | The defaults `title="Command palette"` and `description="Search for a command to run."`                                              | Two more English strings in the accessibility tree, not routed through the labels context. They can be overridden only by a prop at the call site — which is exactly the method the labels context's JSDoc says “no consumer has ever used”                                                                                                                                                                                                                                                                             |

> **What this dossier leaves uncovered**
>
> Nothing was verified **by execution**: the package's tests were not run and the application was not brought up — this environment has no installed dependencies. Every number was obtained statically, from the sources and the manifests. The largest CLI components in which no divergence from upstream was found were not read line by line (`calendar.tsx`, `command.tsx`, `drawer.tsx`, `input-group.tsx`, `collapsible.tsx`) — for them the inventory gives the purpose, the props and the consumers but not the internal markup. Token contrast was taken from the comments in `styles.css` rather than recomputed.

<details>
<summary>Why the numbers in this artifact were counted rather than copied, and how to recount them</summary>

Every figure in the header is an enumeration of the working tree at the time of writing, not a fact about the library — and each has now drifted at least once, which is why the method is written down rather than the number alone. **49 component files**: non-spec `.tsx` under `src/lib/components/ui/`. **234 exported symbols**: named re-exports in `src/index.ts`, counted per identifier rather than per `export` statement; there is no `export *`. **25 dependencies, 16 Radix**: keys of `dependencies` in the package manifest, Radix being the `@radix-ui/` prefix. **19 consuming packages**: manifests under `packages/` and `apps/` that declare `@orthacms/design-system`, excluding its own.

**131 `--color-*` declarations** needs a correction rather than a recount, because it was attributed to the wrong package. This package's own `styles.css` holds motion rules and nothing else — **no colour token at all**. The palette lives in `apps/admin/src/styles.css` and, byte for byte, in the scaffolder's template: 131 declarations over 76 distinct names, which is what a light value and a dark value per token comes to. The count is not pinned, but the property that matters is: `library-conventions.spec.ts` reads both palettes and asserts every token is paired across the themes unless it belongs to one of three families that are the same colour in both on purpose.

The test-suite count that used to sit here has been removed rather than corrected. It drifted three times, and "23 suites · 189 cases" told a reader nothing that "there are tests" does not — unlike **0 routes, tables and permissions**, which is a structural claim about what this package is.

</details>

---

**A dossier on a library, not a plugin.** The series' frame is kept, but the sections a component library does not have — permissions, tables, an HTTP API, a lifecycle, configuration — are replaced by the ones that are load-bearing for it: tokens and theming, the complete component inventory, the authors' rules and accessibility. The main section is the fourth: 48 files, 233 exports, five groups by purpose, with the actual usage counts across the repository.

The source is the source code of `packages/design-system/**`: all 48 components, `src/index.ts`, `src/styles.css`, `utils.ts`, `appearance/`, `labels/`, `hooks/`, the test suites, `components.json`, `package.json`, `vite.config.mts`, `tsconfig.lib.json`; plus `apps/admin/src/styles.css` (the token values), `packages/bootstrap/admin` (mounting the providers), `tools/release/pack.mjs` (publishing), and the `shadcn` and `accessibility` skills. The `AGENTS.md` files were used as the frame, but every claim was checked against the implementation — the divergences are gathered in section 13.
